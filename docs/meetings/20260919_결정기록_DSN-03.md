# DSN-03 2차 회의 — 안건과 결정 기록

| 항목 | 내용 |
|---|---|
| 일시 | 2026-09-19(토) — **회의는 열지 못했다**(교육장 18시 마감·시간 부족). 안건은 **PM(황인재)이 직접 결정**하고 팀에 공지한다. 이의는 9/20 09:30 브리핑에서 |
| 목표 | 40분 안에 아래 안건을 **전부 결정**한다. 못 정한 것은 담당·기한을 적고 넘어간다 |
| 회의 뒤 | DSN-04(황인재): 결과를 IRD v3.1 · `cobot_api` · `cobot_msgs` · SDD · 프롬프트 · 일정표에 반영 → 전원 `git pull` + `cbc` |
| 쓰는 법 | 안건마다 **PM 제안**이 있다. 이의가 없으면 "결정" 칸에 ✅만 적는다. 다르게 정하면 그 내용을 적는다 |

상태: 🟢 **PM 결정(9/19 16시 · 9/20 아침 추가)** — B1~B3·B7·B11·B12·D1~D3·A 전체 결정 · 나머지(B4~B6·B8~B10·C·C5)는 급하지 않아 보류(필요할 때 결정).

## A. 확인만 하면 되는 것 (5분) — 오늘 PM이 먼저 정한 것
근거는 [9/18 결정 기록](20260918_결정기록_구조_인터페이스.md)의 Q1~Q7 · S1~S5. 이의가 있으면 이 자리에서 다시 정한다.

| # | 내용 | 결정 |
|---|---|---|
| A1 | 담당 표기 `A(B)` = A 주도·B 참여, 한 가지 일의 주인은 한 명 (V-04·V-15 = 한석형 주도, V-16 = 민범진 주도, ENV-03 = 황인재 주도 …) | ✅ PM 결정 9/19 — 확정(팀 공지로 확인을 갈음) |
| A2 | `cobot_common`은 사람별 파일(9/19 오후 변경): `bootstrap.py`·`config.py`·`__init__.py`·**`motion.py` 황인재** · **`gripper.py`(신규)**·`weigh.py` 민범진 · `force.py` 박진용. 두산 함수는 함수 안에서 `dsr()`. **한석형 = 티칭·`cell.yaml` 값·실기 검증·F1 기능 함수** · 그리퍼 검증 V-01·V-05·V-23 = 민범진(9/20 오전 첫 순서) — 결정 기록 W2~W5 | ✅ PM 결정 9/19 — 확정(팀 공지로 확인을 갈음) |
| A3 | `cell.yaml`은 한석형 혼자(골격은 main에 있음, 값 67개 비어 있음) · `params.yaml`은 자기 절만 | ✅ PM 결정 9/19 — 확정(팀 공지로 확인을 갈음) |
| A4 | 🚨 기본은 격리(`ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST`), PC 두 대 통합 때만 `team60` — **네 명 모두 `.bashrc`에 넣었는지 확인** | ✅ PM 결정 9/19 — 확정(팀 공지로 확인을 갈음) |
| A5 | Ctrl+C는 `cobot_common.init()`이 단독 처리. `flow_node`·rig는 `try/finally`만 | ✅ PM 결정 9/19 — 확정(팀 공지로 확인을 갈음) |
| A6 | PR: 문서만 바뀐 PR은 자동 merge, 코드 PR은 PM 에이전트가 읽고 승인·merge 또는 거절 코멘트 | ✅ PM 결정 9/19 — 확정(팀 공지로 확인을 갈음) |

## B. 오늘 정해야 하는 것 (25분)
| # | 안건 | 배경 | PM 제안 | 결정 |
|---|---|---|---|---|
| B1 | **반납 구역 방식** — 구역 + 탐색 파지(`RET_B`/`RET_C`, 탐색점 순회, `EMPTY_ZONE`) vs 고정 슬롯 | V-01(파지 폭 3상태) 결과가 근거. F1-02(9/20 오전)가 이 결정을 기다린다 | V-01에서 3상태 간격 ≥ 6 mm가 나오면 **구역 + 탐색 파지**(현재 문서 그대로). 안 나오면 고정 슬롯으로 범위 방어(SDD §9.9) | ✅ **고정 슬롯** — 구역마다 지정된 슬롯 2개에 겹치지 않게 놓고 순서대로 집는다. `pick` 서명·구역 ID·`EMPTY_ZONE`·YAML 키는 그대로(`offsets_mm` = 슬롯 위치). 겹친·어긋난 용기 대응은 범위에서 제외. V-14는 "고정 슬롯 파지 성공률"로 |
| B2 | **그릇 파지 방식** | 박진용 실측: 그릇 외경 114 mm > RG2 최대 폭 110 mm → 지름 방향으로 못 잡는다([CELL-02a 기록](../test_logs/20260918_CELL-02a_용기치수측정.md) §3). SDD §4.3의 `BOWL` 프리셋 예시는 실측 전 값 | **테두리(벽) 파지** — 폭·힘·접근 높이는 한석형이 V-01에서 정해 `cell.presets.BOWL`에. 털기·헹굼 때 버티는지는 V-16 | ✅ **그릇 옆면(벽)을 세로로 파지** — 외경 114 mm > 그리퍼 최대 폭 110 mm. 핑거가 벽 안팎을 집는다. 폭·힘·벽까지의 오프셋은 V-01·V-16에서. **파지 성공은 그릇도 파지 폭으로 가른다** — 옆면 세로 파지 폭 ≈ 2 mm, **빈손과 구분되는 것까지 검증**(9/19 황인재) → 무게로 가르는 대안은 뺐다. 그릇 전용 허용 오차와 닫는 목표 폭(기대 폭보다 작게)은 V-01에서 |
| B3 | **그리퍼 현재 폭을 읽는 경로** | 강사 배포 드라이버는 `OnRobotRGInput` 토픽을 내지 않는다. 나오는 것은 `/onrobot_joint_states`(JointState)뿐 → V-05 결과 | `motion.py`의 `setup_io(node)`에서 `/onrobot_joint_states`를 구독해 관절각 → 폭(mm) 환산(드라이버의 환산식 그대로). 안 되면 파지 성공 판정을 "명령 폭과 실제 정지 여부"로 단순화 | ✅ 제안대로 — 강사 드라이버(관절각 → 폭 환산, 힘 2.5 N 계단) 먼저, 안 되면 Compute Box XML-RPC를 읽기부터. 구현은 `gripper.py`(민범진) |
| B4 | **스펀지 홈 재파지** — 홈에 안착된 용기를 다시 잡을 때 폭으로 판정 가능한가 | V-15는 9/20 오후(티칭 2차 뒤)라 오늘은 방식만 | 방식은 그대로 두고 V-15 결과로 확정. 안 되면 재파지 폭 판정 생략(위치만 믿음) | ⏸ 보류 — 급하지 않음. PM 제안을 기본값으로 쓰고 필요할 때 결정 |
| B5 | **HMI 설계** — 화면 구성 · `FlowState`/`FlowEvent` 필드 · 화면 방식 | F4-00 초안(황인재)이 **9/20 오전**으로 밀렸다(오늘은 F1 골격·`motion.py`가 먼저). 9/18에 화면은 **Next.js 정적 내보내기 + FastAPI 브리지**(프로세스 1개)로 하기로 했으나 문서에는 아직 `static/index.html` | 오늘은 방식(Next.js + FastAPI)과 "필드는 **추가만**(삭제·이름 변경 없음)"만 확인하고, 초안은 9/20 브리핑에서 | ⏸ 보류 — 급하지 않음. PM 제안을 기본값으로 쓰고 필요할 때 결정 |
| B6 | **추가 토픽 `/cell/force`(10 Hz, 닦는 동안) · `/cell/grip_width`** 와 모니터·기록 노드 추가 여부 | 강사 산출물(힘 그래프)용 제안. 노드를 늘리면 구조 결정(노드 2개)과 부딪힌다 | 토픽 2개는 **추가**(발행은 `flow_node`의 통신 노드, 값은 기능 함수가 저장한 최신 값). **노드는 늘리지 않는다** — 기록은 hmi_bridge의 SQLite로 | ⏸ 보류 — 급하지 않음. PM 제안을 기본값으로 쓰고 필요할 때 결정 |
| B7 | **실패 코드·정책** | `params.yaml`의 `flow.policy` 초안: `EMPTY_ZONE: next_zone` · `LEFTOVER_REMAIN`·`SEAT_FAIL: isolate` · `FORCE_LIMIT`·`TIMEOUT`·`RACK_JAM`·`TOOL_FAIL: retry:1->isolate` · `RACK_FULL`·`ROBOT_ERROR: pause` | 초안 그대로. 어떤 실패에서도 **툴은 홀더에 반납, 로봇은 안전 높이**(AGENTS §4) | ✅ 황인재 9/20 — 정책 표는 초안 그대로 · **격리 마무리 순서 = 툴 반납 → 용기를 ISOLATE에 놓기 → HOME** · **ROBOT_ERROR = 그 자리 정지 + PAUSED + 사람이 복구**(두산 DR_Error 뒤에는 그 프로세스가 명령을 못 보낸다 → `release_force.py` → 재시작) — SDD §7 |
| B8 | **YAML 키 이름 규칙** | SDD §4.3의 PM 제안(소문자 snake_case + 단위 접미사, ID는 대문자 그대로, 키 추가는 팀 공유) | 제안 그대로 + `flow.leftover_max_rounds` 키 추가(IRD §8 `leftover_loop(kind, 2)`의 2) | ⏸ 보류 — 급하지 않음. PM 제안을 기본값으로 쓰고 필요할 때 결정 |
| B9 | **정지 방식** | `bootstrap.py`의 `shutdown()`이 `move_stop`을 `DR_QSTOP`(Stop Category 2)으로 부른다. Virtual에서는 모션 중 Ctrl+C 정지·재실행 확인 | 박진용(안전 담당)이 값 확인. 실기 확인은 V-24(선택). 급한 정지는 E-Stop | ⏸ 보류 — 급하지 않음. PM 제안을 기본값으로 쓰고 필요할 때 결정 |
| B10 | **실기에서 rig를 손으로 돌릴 때 속도** | `PREWASH_VEL_SCALE`이 없으면 `vel_scale = 1.0`(= `cell.limits`의 속도 % 그대로). 런치는 기본 0.3 | 규칙으로: **실기 rig는 항상 `PREWASH_VEL_SCALE=0.3`부터**(AGENTS §3 규칙 1). 코드로 강제할지는 한석형 `motion.py` 구현 때 | ⏸ 보류 — 급하지 않음. PM 제안을 기본값으로 쓰고 필요할 때 결정 |
| B11 | **털기·물털기용 관절 이동 함수가 없다** (민범진 올림) | `shake`는 J5/J6 **관절 왕복**이다(SDD §5.3). 그런데 `cobot_common` 공용 함수에 관절을 움직이는 것이 없다 — `move_to`는 스테이션 고정, `move_rel`은 직교좌표, `periodic_search(amp, period, duration)`는 축 인자가 없다. **F2-01·V-07(9/20 오전, 민범진 첫 로봇 작업)이 이것을 기다린다.** 덧붙여 `shake`의 미끄러짐 판정(동작 전후 폭 비교 → `GRIP_FAIL`)은 **B3 결과에 달려 있다** — 폭 환산이 안 맞으면 판정 방식을 바꿔야 한다 | **민범진 제안**: 둘 중 하나. (a) `periodic_search`에 **축 인자**를 더해 `periodic_search(axis, amp, period, duration)` — 박진용 `force.py` / (b) `motion.py`에 **관절 상대이동** `move_joint_rel(...)` 신설 — 한석형. 어느 쪽이든 **파일 주인이 만들고 나는 부르기만** 한다(AGENTS §2). 오늘 못 정하면 F2-01의 `shake`를 9/20 **오후**로 미루고 오전에는 `weigh`·`leftover_loop`만 한다 | ✅ 황인재 9/20 확인 — `move_joint_rel(joint, delta_deg, *, time_s=None, carrying=True)` (PR #15·#16, 황인재 `motion.py`) |
| B12 | **`mock_f2`가 문서에 없는데 런치는 부른다** (민범진 올림) | IRD §10과 일정표 INF-03 산출물은 `mock_f1`·`mock_f3` **둘뿐**인데, `prewash_mock.launch.py`는 `use_mock='f1,f2,f3'`로 부르고 `config.MOCKABLE`에도 `f2`가 있다. 지금은 `sense.py`가 껍데기라 안 드러나지만 **F2-01로 속을 채우면 로봇 없는 런치가 깨진다**(실제 `sense`가 `cc.move_to`를 부르게 되므로) | **민범진 제안**: `mock_f2`를 **둔다**(이미 만들어 브랜치에 있음 — 내 모듈의 가짜라 소유권 문제 없음). IRD §10과 INF-03 산출물에 한 줄 추가. 반대면 `prewash_mock.launch.py`의 `use_mock`을 `f1,f3`로 바꾸고 `MOCKABLE`에서 `f2`를 뺀다 | ✅ 황인재 9/20 — `mock_f2`를 IRD §10에 추가(코드는 PR #8부터 있음) |

## C. 일정 (10분)
| # | 안건 | PM 제안 | 결정 |
|---|---|---|---|
| C1 | **로봇 1대를 3명이 — 1시간씩 교대** (주말 저녁이 없어져 모든 로봇 칸이 3명 교대다) | 9/20 오전: 민범진(그리퍼 세션 → V-02·weigh) → 박진용(V-03 → V-18) → 한석형·박진용(티칭 2차 + V-22 → F1-01) / 9/20 오후: 한석형(F1-02·V-14) → 박진용(F3-02) → 민범진(V-07·V-16 → F2-01) / 9/21 저녁: 한석형(V-04·V-15 → F1-05) → 박진용(V-10 → F3-03) → 민범진(F2-01·F2-02). 자기 차례가 아닐 때는 Virtual·문서 | ⏸ 보류 — 일정표 v5.0 기준으로 진행, 9/20 브리핑에서 |
| C2 | **재계획 확인(일정표 v5.0)** — 주말 저녁 없음 · 게이트 G1 9/20 오후 · L1 9/22 오후 · L2 9/23 오전 · L3 9/23 오후 · 동결 9/23 저녁 그대로 | 밀리면 SDD §9.9 범위 방어를 바로 쓴다: 4개 연속 → 2개(그릇 1·컵 1) · 실패 주입 4종 → 2종 · 컵 닦기는 도전 과제. 9/22 오후(G2)에 PM이 판정 | ✅ 일정표 v5.0 확정(주말 저녁 없음·게이트 조정) |
| C3 | 오늘 진척 — V-01 · V-02 · V-05 · V-23 · CELL-04 · PKG-01(F1·F2) · FLOW-01 · V-20 · CELL-02 · V-12 | 각자 한 줄씩: 끝남 / 진행 중(몇 %) / 막힘(이유) → 일정표 반영 | ⏸ 보류 — 일정표 v5.0 기준으로 진행, 9/20 브리핑에서 |
| C4 | 9/21(월) 오후 중간점검 발표(10장) 분담 | 황인재 취합. 각자 자기 기능 1장(무엇을·어디까지·남은 위험) — **9/20 오후 17시까지**(주말 저녁 없음) | ⏸ 보류 — 일정표 v5.0 기준으로 진행, 9/20 브리핑에서 |
| C5 | **F1 기능 함수가 한석형에게 몰려 있다**(F1-01~05가 9/20~22, 시연 필수 경로 — 파지·적재가 안 되면 시연이 성립하지 않는다) | 한시적 예외: 함수 **초안**(로직·호출 순서)은 속도가 빠른 세션(황인재 F4 세션 또는 민범진)이 SDD §5.2대로 Virtual에서 쓰고, **한석형이 주인으로서 실기 튜닝·값 확정·PR 승인**. 9/20 오후 F1-02 진행을 보고 9/20 17시에 발동 여부 결정 | ⏸ 보류 — 일정표 v5.0 기준으로 진행, 9/20 브리핑에서 |

## D. 회의 전에 올라온 요청 (9/19 14:35 기준 GitHub)
| # | 안건 | 배경 | PM 제안 | 결정 |
|---|---|---|---|---|
| D1 | **힘 함수 인터페이스 변경 — [이슈 #7](https://github.com/hwang-injae/rokey_9_pjt1_D2/issues/7)** (박진용) | ① `cell.yaml`에 `cell.force` 절(순응 강성·접촉 하강 단계·속도·후퇴 속도·절대 힘 상한 등 8개 키, 값은 V-03 뒤 박진용이 전달) ② `move_rel(dx, dy, dz, frame, *, vel_mm_s=None, acc_mm_s2=None)` 선택 인자(접촉 하강은 아주 느려야 한다) ③ `force.py`에 `read_force()` · 예외 `ForceLimitError` · `MotionTimeout` 추가 | **수락**. `cell.force` 키 골격은 **황인재**가 추가(INF-02에 포함, `test_config`도), 값은 **박진용이 그 절만 PR**. `move_rel` 선택 인자는 **황인재**의 `motion.py`에(한석형 부담 없음 — 결정 기록 W3·W5). 두 예외는 **기능 함수 안에서** `FORCE_LIMIT`·`TIMEOUT` 코드로 바꾼다(flow까지 새어 나오면 `ROBOT_ERROR`) → SDD §3.1·§4.3 반영은 황인재 | ✅ **수락** — `cell.force` 키 골격·`move_rel` 속도 인자 = 황인재(`motion.py` PR), 값 = 박진용이 그 절만. 공용 힘 함수는 예외로 알리고 기능 함수가 코드로 바꾼다 |
| D2 | **그리퍼 폭·힘 경로 제안서** (박진용, [docs/ref](../ref/20260919_제안_RG2_폭_힘_경로.md)) — B3의 근거 자료 | A 강사 드라이버만(관절각 → 폭 환산, 힘은 2.5 N 계단) / B Compute Box XML-RPC(폭 mm·힘 N 절대값·잡음 감지, 실기 전용·동시 연결 미확인) / C 섞기 | **A로 먼저 V-05(30분 순서는 제안서 §5)**, 폭 환산 오차 > 2 mm거나 힘 2단계가 안 되면 B를 읽기부터 추가. 구현은 **`gripper.py`(민범진, INF-02d)** 한 곳. V-05·V-23·V-01도 민범진이 9/20 오전 첫 로봇 순서에. F3용 **현재 폭 읽기 함수** `grip_width()`를 `gripper.py`에 둔다 | ✅ 제안대로(B3와 같음) — `grip_width()`는 `gripper.py`에 |
| D3 | 민범진이 올린 안건 **B11**(털기·물털기용 **관절 이동 공용 함수**가 없다 — F2-01·V-07이 9/20 오전에 기다림) · **B12**(`mock_f2`를 문서에 넣을지 — 런치는 `use_mock='f1,f2,f3'`로 부른다) | 민범진 브랜치 `beomjin/20260919-PKG-01-package-skeleton`의 이 문서 수정분에 있다(PR이 merge되면 B 표에 들어온다) | B11: `motion.py`에 **관절 상대 이동 함수 1개 추가**(예 `move_joint_rel(joint, delta_deg, vel_pct)`, **황인재**) — 9/20 오전 PR. B12: `mock_f2` **둔다**(IRD §10·INF-03 산출물에 추가) | ✅ 황인재 9/20 — B11·B12 둘 다 수락(위 행 참고) |

## 회의 뒤 할 일 (회의에서 채운다)
| 작업 | 담당 | 언제 |
|---|---|---|
| DSN-04 결과 반영(IRD v3.1 · cobot_api · cobot_msgs · SDD · 프롬프트 · 일정표) | 황인재 | 9/19 저녁 ~ 9/20 오전 |
| | | |

## 9/20 추가 결정 (황인재)
| # | 결정 | 내용 |
|---|---|---|
| E1 | **티칭 자세 = 그 기능이 동작을 시작하는 자세** | `WEIGH` = 무게를 재는 자세(안전 높이 이상 권장), `WASTE` = 잔반통 위에서 터는 자세, `RINSE`·`SOAP` = 수조 위에서 담그기를 시작하는 자세. `cc.move_to()`가 돌려주는 남은 높이만큼 먼저 내려간 뒤, `depth_mm` 등은 티칭 자세 기준. 시험에서 맞지 않으면 좌표가 아니라 **파라미터를 고친다**(SDD §5.3) |
| E2 | 좌표 작업(CELL-04) | 9/19 그릇 쪽까지 → **9/20 오전에 컵 쪽까지 끝낸다**. V-19(도달 범위)는 좌표가 끝난 뒤. CELL-01(구역·팔레트 배치)은 좌표 측정과 함께 진행, CELL-03(잔반통·수조 배치)은 9/20 오전 |
| E3 | PM 에이전트 근무 시간 | PR 확인·일정표 확인은 **평일 06~22시 · 주말 06~18시**에만 |
| E4 | 좌표 이전 방식 (9/20 15:55) | 한석형이 티칭한 좌표(브랜치 `seokhyung/20260919-CELL-04-teaching` 의 스크립트 안 상수 약 25개)를 **황인재가 받아 작업 파일(`cell.yaml` 등)로 만들어 넘겨준다**. 파일의 주인은 그대로 한석형 — 한석형은 빠진 자세(WEIGH·SOAP·ISOLATE·구역 두 번째 슬롯)와 실기 확인에 집중한다. 팔레트 컵 2칸 코드 변경(`RACK_SLOTS` 등 5곳)도 같은 PR 에. 9/19 안건 C5(F1 을 돕는 한시적 예외)의 적용 |
| E5 | 정지 방식 (9/20 15:30) | 일시 정지 = 이동 도중 즉시 멈춤 → 재개하면 하던 동작을 이어서(V-24, PR #34) · 사람이 확인해 마저(resume) 또는 접기(`/flow/abort` 신설) · 웹 HMI 는 추석에도 집에서 — IRD §6 · SDD §5.1 |
| E6 | **그릇 닦기 = 고정 좌표 방식** (9/20 16:30) | 벽 찾기를 그만둔다 — 수세미가 물러서 닦는 도중에는 벽이 잡히지 않는다(수세미가 로봇 순응보다 훨씬 물러 "못 따라간 거리"가 안 생기고, 바닥 마찰 5~14 N 이 벽 신호 1~2 N 을 덮는다 · V-03 기록 §3-C). ① **반지름 고정** = (그릇 안지름 110 − 수세미 90) / 2 + 벽 누름(설정값) ② ~~높이도 고정(Z 59.0) · 바닥을 힘으로 찾지 않고 목표 힘도 유지하지 않는다~~ → 🔄 **9/20 밤 E13 으로 정정**(박진용 실기 검증): 바닥은 `contact_down` 으로 찾고, 벽면에서는 목표 힘 1.5 N 을 유지한다. ③ 그래도 **힘 감시는 남긴다**(NFR-01 · AGENTS §3 규칙 2): 마지막 하강 구간은 천천히 + 누르는 힘 상한 10 N, 닦는 동안 누르는 힘 10 N · 옆 힘 25 N 을 넘으면 즉시 후퇴(`FORCE_LIMIT`), 힘 로그 저장. ④ 값은 코드가 아니라 설정에 — **닦는 높이 = `cell.yaml` `beds.SPONGE_BED_B.wash` 끝점의 z**(#36 양식: `cc.move_to(…, point='wash')` 가 돌려주는 높이만큼 내려간다 · 지금 값 47.0 은 한석형 티칭 → 59.0 과 맞춰 본다), 반지름·바퀴 수·힘 상한은 `params.yaml` `f3.wipe_bowl`. ⑤ 전제: 그릇이 받침에서 밀리지 않을 것(손으로 5 N 쯤 밀어 안 움직임) · 툴을 늘 같은 높이로 잡을 것(9/20 파지 기준). → FR-08·SR-08·TR-05 · SDD §4.3·§5.4·§8(V-03)·TC-06 수정 |
| E7 | **이동에서 안전 높이 경유를 삭제** (9/20 17:00 · 한석형 요청, 황인재 승인) | `cc.move_to` 가 "먼저 안전 높이로 올라가고 · 안전 높이에서 멈추는" 동작을 없앤다 → 티칭한 자세(접근점이 있으면 접근점)로 **직접** 간다. 이유: 팔레트 적재처럼 그리퍼를 눕혀 들어가는 자세는 안전 높이에서 자세가 안 나온다. 한석형의 좌표는 "자세에서 자세로 직접" 가는 경로로 티칭됐다(#36 본문의 경로 차이). 대신하는 것: ① 접근점(`approach_posx`) — 끝점 바로 위의 "옆으로 움직여도 되는 높이" ② 기능 함수는 **내려간 만큼 다시 올라온 뒤** 끝낸다 ③ 실기에서 구간마다 저속 확인(V-22 — 흐름 순서대로 앞 자리 → 다음 자리). ✅ `safe_retreat()`(접촉 동작 뒤 후퇴)는 그대로 둔다 — `cell.limits.safe_z_mm` 은 **후퇴 높이**로만 남는다(황인재 확인 9/20 17:10 — "후퇴 높이는 남기고 안전 높이 같은 것은 없앤다": 닦다가 힘 상한에 걸렸을 때 그릇 밖으로 올라오는 높이가 필요하다 · 팔레트처럼 그보다 높은 자리에서는 움직이지 않으므로 적재 자세와 부딪히지 않는다). 구현: F4 세션 PR(motion.py · 시험 · rig) |
| E8 | 기능 함수 약속에 `kind` 선택 인자 (9/20 17:00 황인재 승인 — `move_to` · 같은 이유로 PM 이 `place`·`soap` 까지 넓힘) | `f1.move_to(station, carrying, kind=None)` · `f1.place(station, kind=None)` · `f3.soap(count, kind=None)` — 좌표가 종류별(WEIGH·WASTE·SOAP·RINSE·ISOLATE)이 돼서(#36) flow 가 종류를 넘겨야 한다. 기본값 None 이라 기존 호출은 그대로 돈다. `contracts.py` · `mock_f1.py`·`mock_f3.py` · `handling.py`·`wipe.py` 골격의 서명을 **같이** 바꿨다(`check_api` 가 인자 이름을 그대로 비교해서 따로 바꿀 수 없다). 남은 것: 민범진 — flow 호출부에 kind · `sense.py` `_goto` 에 kind / 한석형 — F1 구현 때 `cc.move_to(station, carrying, kind)` 로 넘기기 |
| E9 | 한석형 보고(9/20 17:10) — ✅ ①은 황인재 확인(17:25: "반납 구역이 내리막길이라 꺼내면 뒤에서 내려온다 — 파지 자리는 1개") | ① **반납 구역은 한 자리 공급 구조**(한 개를 집으면 뒤 용기가 같은 자리로 내려온다) → 구역마다 집는 자세 1개, 슬롯 2 는 없앤다(9/19 "고정 슬롯 2개"를 바꾸는 것 — flow 는 같은 자리에서 두 번 집는다 · 조건: 다음 용기가 같은 자리에 오는 반복 정밀도 ±3 mm 확인). ② **WEIGH = 픽 자세에서 Z +100**(그릇 `[531.78, 8, 158.1, 102, 180, 102]` · 컵 `[564.9, 189.6, 147.4, 146.92, 180, 151.5]`) — 새로 찍지 않는다. ③ 툴 집기·SOAP·툴 반납은 툴·홀더 형상이 확정된 뒤에 찍는다(날짜 요청함). ④ 팔레트에서 나올 때: 랙 안에서는 직선으로 되돌아 나온 뒤 밖에서 HOME 관절 이동 — 그릇 칸의 "랙 밖 자리"를 접근점으로 찍는다. ⑤ 🚨 limits·motion·presets 값은 답이 없었다 — 9/21 저녁 세션 전까지 다시 요청 |
| E10 | HMI 접속 범위 (9/20 17:25) | **웹 화면은 이 PC 의 브라우저에서만 접속한다** — `hmi.host: 127.0.0.1`(PR #41 의 기본값이 곧 최종). 시작·중단 버튼을 같은 와이파이의 다른 기기가 누를 수 없게. 태블릿용 비밀번호(F4-02b)는 하지 않는다 |
| E11 | 중단(`/flow/abort`)의 첫 이동 = HOME (9/20 17:25) | 정리 순서: **HOME** → 툴 반납 → 용기를 격리 구역에 → HOME → 다음 용기. 이유: E7 로 안전 높이 경유가 없어져 임의의 자세에서 다음 자리로 곧장 가게 되므로, 티칭 경로의 출발점인 HOME 에서 시작한다(IRD §6) |
| E12 | `GRIP_FAIL` 정책 = `pause` (9/20 17:25) | 털기·담금 중 미끄러짐·빈손이면 **멈추고 사람이 확인**한다(격리 아님). `params.yaml` `flow.policy.GRIP_FAIL: isolate → pause` 는 민범진이 자기 절에서 고친다 · IRD §8 · SDD §7 에 행 추가 |
| E13 | **E6 ② 정정 — 바닥은 힘으로 찾고, 벽면은 힘제어 1.5 N** (9/20 밤 · 황인재: "박진용이 직접 실기로 검증하고 확인한 사실만 얘기한 것이므로 요청대로 처리") | 닦기 절차 = 접근점 → 끝점 10 mm 위까지 빠르게 → **`contact_down` 으로 바닥 찾기**(시작 힘 대비 2 N) → 순응 ON(찾은 자리 그대로, 더 누르지 않음) → 바닥 나선 1회(2.8바퀴 · 반지름 14 mm · 3 s · 비틀기·힘제어 없음 — 나선은 툴 Z 축 모션이라 Z 힘제어와 같은 방향) → **힘제어 ON 1.5 N**(공중 기준값 보정) → 벽면 원호 반대 방향 3바퀴 + 손목 ±18° → 힘제어만 OFF(순응 유지) → 중심 복귀 → 순응 OFF → `safe_retreat`. **이유**: 수세미가 물러 접촉 깊이가 실행마다 **12~17 mm 로 달라**(V-03 §G) 티칭 높이 하나로 맞출 수 없다 — 9/20 14회차에서 눈으로 확인한 절차를 그대로 쓴다. **E6 에서 그대로 남는 것**: 벽은 찾지 않는다(반지름 = (안지름 − 툴 지름)/2 + 벽 누름) · 힘 상한 10 N · 옆 힘 25 N · 힘 로그. 함께: ③ `wipe_cup` = 바닥 찾기 → 띄우기 → **Move Periodic 한 명령으로 위아래 + 비틀기 동시** → 아래쪽 끝에서 종료 ④ **SR-09 의 "J6 ±180°" → "툴 축 회전, 각도는 V-10 에서 확정"**(±180° 를 왕복 진폭으로 주면 한 번에 360° 라 손목 한계·속도 위험) ⑤ **그릇 받침 유격은 그대로 둔다** — CELL-02b 완료 기준에서 뺀다. → FR-08 · SR-08 · SR-09 · SDD §4.3·§5.4 · V-10 · 구현 PR #43 |
| E14 | **좌표 티칭은 1차로 끝내고 2차 티칭 일정은 잡지 않는다 — 이후 수정은 요청 기반** (9/21 10:20 황인재) | 황인재(F4)가 **9/21 안에 좌표를 한 번에 전부** 찍어 `cell.yaml` 에 넣는다(CELL-04·04b 마감). 그 뒤 F1~F3 이 기능 함수를 만들다가 좌표가 이상한 곳을 만나면 **그때 황인재에게 요청**하고, 황인재가 확인해서 고친다 — 통합 과정에서 나오는 것만 고치는 방식. 일정표에서 "남은 티칭 / 2차 티칭" 칸을 없앤다. **이유**: 어느 자세가 어떻게 틀렸는지는 실제로 기능 함수를 돌려 봐야 나온다 — 미리 잡아 둔 2차 티칭 시간은 고칠 것을 모른 채 로봇을 묶고, 정작 필요한 수정은 그 시간 밖에서 생긴다. **요청 방법**(단톡방 또는 PR 코멘트에 3줄): ① 어느 자리인지 **설정 키 이름**으로(예 `stations.WASTE.BOWL`, `racks.BOWL.slots[2].posj`) ② 무엇이 이상한지(몇 mm 어느 방향 · 자세가 안 나온다 · 가는 길에 걸린다 · 관절이 크게 돈다) ③ 지금 막혀 있는지, 나중이어도 되는지. 급하면 바로 로봇을 잡고 고치고, 급하지 않으면 모아서 한 번에 다시 찍는다. → 일정표 v10.0 · AGENTS §2 🔄 **9/21 오후 보완(황인재 · PR #51)**: 마감한 좌표는 **임시 좌표**다 — 요청하고 기다리는 대신 **기능 담당이 직접 수정·삭제한 뒤 보고**한다(① 설정 키 ② 무엇이 어떻게 이상했는지 ③ 무엇으로 바꿨는지 · 실기 근거). 단 `HOME` 은 바꾸지 않는다(E17). **우선순위: 구현 방식 > 임시 좌표**(황인재) — 팀원들은 좌표를 받기 전부터 기능을 만들고 있었다. 좌표가 기능에 맞춰 바뀌는 것이지, 기능을 좌표에 맞출 필요는 없다. 만드는 방식에 따라 좌표가 바뀌거나 **아예 필요 없어질 수 있다**(실제 사례: E17 — 닦기를 HOME 기준 상대 이동으로 바꾸자 `SPONGE_BED_B/C.wash` 2개가 통째로 필요 없어졌다). 🔁 같은 날 황인재 확인 2건: HOME 경유(E15 ④)는 "필요할 때만 넣으면 괜찮다" — ④ 의 "뺄 수 있다" 와 같은 뜻 · 팔레트는 충돌 때 **실제로 밀렸고 손으로 원위치**했다 → RACK_B1·B2 는 "복귀 뒤 미확인"(F1-04 첫 적재 실기 전에 2분 재확인 · 바닥 테이프 표시 권장). |
| E15 | **경로 제약 3가지 — 몸통을 가로지르지 않는다** (9/21 오전 · 황인재 F4 세션 실기) | ① **잔반통(로봇 뒤) ↔ 앞쪽 자리(스펀지 홈·저울·반납 구역) 사이는 `HOME` 을 거친다.** 잔반통 그릇 자세를 로봇 뒤쪽으로 옮기면서 생긴 제약 — 곧장 가면 로봇 몸통을 가로지른다. **민범진**: `f2.leftover_loop`(털기 반복)에 경유 추가 · **한석형**: `f1.place`·`move_to` 부르는 순서에 반영. 순회 도구(`rig_coords.py`)에는 이미 넣었다. ② **팔레트 칸은 `cell.rack.via`(HOME 의 x·y·방향 그대로 z 338)를 거친다** — 곧장 가면 올라가며 손목(J4)이 휘둘려 몸통에 부딪힌다(실기 2회 실패). 한석형 스크립트에 있던 자세가 `cell.yaml` 로 옮길 때 빠진 것. **한석형**: `f1.rack_place`(F1-04) 가 칸 접근점 전에 한 번 거치게. 그릇 칸은 놓은 뒤 `exit_rel_mm`(y −25 → z +100) 순서로 빠져나온다 — 곧장 올라가면 그리퍼가 팔레트에 걸린다. ③ **팔이 쭉 펴지는 자세(J3 ≈ 0°)를 티칭하지 않는다.** 🔄 **9/21 08:40 케이블 꼬임의 원인 정정** — 손목(J6 163° 회전)이 방아쇠였지만 근본 원인은 **팔꿈치 특이점**이었다: 그때 잔반통 그릇 자세가 J3 = **1.7°**(다른 자세는 68~133°) → 직선으로 들어가면 6 mm 앞에서 멈추고 빠져나올 때 손목이 요동친다. J6 163° 는 posj·posx 어느 쪽이든 그 구간에서 비슷하게 난다(재계산 확인). 조치: 잔반통 그릇 = `posj [-180, 0, 90, 0, 90, 0]`(J3 90°·J5 90°·J6 0°)로 재티칭 · `rig_coords.py` 가 도착마다 J3·J6 를 찍고 경고. 🔎 남은 확인: 잔반통↔스펀지 홈 사이 허리 180° 회전에서 손끝이 그리는 원(반지름 366 · 높이 210)과 수세미 홀더(반지름 353 · 꼭대기 z ≈ 128)의 여유 ≈ 82 mm — 9/21 실기에서 눈으로. ④ 🆕 (9/21 오후 추가 · 황인재) **팔레트에서 다른 구역으로 갈 때는 `HOME` 을 거친다** — 팔레트 그릇 칸 → 컵 반납 구역(RET_C)을 곧장 가다가 **로봇이 팔레트에 부딪혔다**(황인재 목격). 팔레트 자세만 J4 101°·J6 −118° 로 크게 비틀려 있어 다음 자리로 가는 관절 이동에서 손목이 풀리며 팔이 팔레트 위에서 휜다. 팔레트 칸끼리는 곧장 가도 된다(실기 확인). **한석형**: `f1.rack_place` 뒤 · **민범진**: flow 의 RACK 다음 단계. 🔓 **기본 규칙이되 뺄 수 있다**(황인재: "팀원들이 기능 구현하면서 필요 없는 동작이라고 판단되면 삭제 가능") — 뺄 때는 실기로 확인한 근거와 함께 PM 에 알린다. 팔레트가 밀렸는지는 확인 대기(밀렸으면 RACK_B1·B2 재티칭). → SDD §4.3·§5.2 |
| E16 | **민범진 9/21 결정 요청 5건 처리 (D-A~D-E)** (9/21 12시대) | **D-A 🔴 파지 폭은 영점을 빼고 판정한다 — ㉠안**(황인재 확정): `cell.presets.<kind>.grip_zero_mm`(빈손으로 꽉 닫았을 때 읽히는 폭) 키를 새로 두고 **판정식만** `grip_width() − grip_zero_mm` 로 바꾼다. `cc.grip_width()` 의 반환값과 `cc.grip()` 의 목표 폭은 드라이버 값 그대로 — 기존 코드가 깨지지 않는다. 근거: 빈손으로 꽉 닫아도 **10.5~10.9 mm** 가 읽힌다(드라이버가 알루미늄 손가락 사이를 재서 고무 핑거팁 두께가 안 빠진다) → 지금 설정의 `BOWL.grip_width_mm: 2.0` 은 도달 불가능한 값이었다. 영점을 빼면 그릇 2.0 = 실측 벽 두께, 컵 75.0 = 실측 상단 지름으로 **설정값이 자로 잰 치수와 같은 뜻**이 된다. 적용: 한석형 `f1.pick` 폭 판정 · 민범진 `f2.shake`·`dip` 미끄러짐 판정 · 값은 V-01 에서 종류별 파지 힘으로 잰다(영점이 힘에 따라 0.2~0.4 mm 달라진다). **D-B 🔴 `ISOLATE.BOWL`·`ISOLATE.CUP` 을 오늘 티칭에 넣는다** — 없으면 `/flow/abort` 정리 순서(HOME → 툴 반납 → ISOLATE → HOME)와 실패 정책의 `isolate` 갈래를 실기로 확인할 수 없고, E14 로 2차 티칭이 없다. `SOAP` 2개와 함께 티칭 앞쪽으로. **D-C 🟡 오후 그리퍼 세션에서 V-02(무게)를 빼고 저녁 2차로** — 60분에 V-05·V-01·V-23 까지가 한계(민범진 산정 50분)이고, **WEIGH 자세가 오늘 티칭에서 바뀌므로 빈 용기 기준값은 티칭 뒤에 재는 것이 맞다**. 오후는 V-05 → V-23 → V-01. **D-D 🟡 V-02 완료 기준을 "±20 g" → "되풀이 폭"으로** — 로봇 하중에 항상 +42~45 g 옵셋이 있는데 잔반 판정이 `측정값 − 빈 용기값` 이라 **옵셋은 상쇄된다**. 우리가 쓰는 성질은 절대 정확도가 아니라 재현성. 새 기준: **같은 추 10회의 (최대−최소) ≤ 20 g**(잔반 임계 50 g 의 절반 이하) · 중앙값의 절대 오차는 **기록만** 한다. 전용 도구는 만들지 않고 `rig_f2.py empty` 로 대신한다. **D-E 🟢 9/20 D1(힘 기준을 0 N 쪽/40 N 쪽 중 어디서 잡나) 철회 확인** — 드라이버가 힘을 기억해(40→35→30→25 N, 프로세스를 끝내도 남는다) "브링업 직후 40 N" 전제가 첫 실행에서만 참이었다. 민범진이 **현재 힘을 읽어서 맞추는 방식**으로 바꿨다(`0893b78`) → 기준을 잡을 필요가 없어졌다. **D2(HOLD → NORMAL 로 되돌릴지)는 그대로 남는다 — V-23 실기 뒤에 정한다.** → SDD §4.3·§5.2 · 일정표 v10.5 |
| E17 | **박진용 9/21 보고 처리 — 정지 방식 확정 · 툴·홀더 확정 · F3 닦기 동작 변경 · 세척 속도 예외** (9/21 13시대) | ① **정지 방식 `stop_mode = 1`(DR_QSTOP) 확정**(박진용 · 안전 담당): QSTOP = 최대 감속 정지(Stop Category 2)라 **서보 전원·위치를 유지** → 일시 정지 뒤 재개가 가능하다. SSTOP 은 감속 시간이 약 1.5배라 서는 거리가 길고, QSTOP_STO 는 전원 차단(Category 1)이라 재개 흐름과 안 맞는다(근거 중급교육1 p.104~105). `bootstrap.py`·`motion.py` 그대로. V-26 실기 때 같이 본다. ② **CELL-02b 툴·홀더는 지금 상태로 확정** — 추가 변경 없음 → 툴 홀더 자세 2종 · SOAP 2개를 바로 티칭한다. ③ **F3 닦기 동작 변경**: 닦기는 `HOME` 에서 시작해 `HOME` 으로 끝난다(F3 가 직접 복귀) · 닦는 자리까지는 HOME 기준 상대 이동(그릇: 바로 아래 135 mm → 3 mm 씩 바닥 찾기 / 컵: z +40 → y +140 → z −40 → 80 mm → 바닥 찾기) → **`SPONGE_BED_B/C.wash` 좌표는 쓰지 않는다**(티칭 불필요). 🚨 대신 **`HOME` 을 다시 찍으면 닦는 자리가 같이 움직인다** — HOME 재티칭 금지. `wipe_cup` 의 `move_periodic` 회전은 손목(J4)을 기울여 쓰지 않고 **직선을 이어 붙인다**(E13 ③ 대체). V-10 값 = 위아래 40 mm · 비틀기 ±18° · 띄우기 2 mm + 세척 속도(SR-09 각도 확정). ④ **세척 동작 속도는 vel_scale 예외 — 황인재 승인("박진용 요청대로 전부 예외")**: F3 가 `params.yaml` `f3` 절의 자기 속도로 관리한다. 이유: 세척은 빠르기가 닦이는 정도를 정하는데 실기 기본 vel_scale 0.3 으로는 안 닦인다. ⚠️ 결과: **0.3 으로 띄워도 F3 닦기는 감속되지 않는다** → F3 첫 실기는 `f3` 절의 속도 값을 직접 낮춰 시작한다(SDD §6 표에 적음). 힘 상한 10 N · 옆 힘 25 N 감시는 그대로. ⑤ **분담 질문 답**: FLOW-02(기록·소모품)와 CELL-03(잔반통·수조 고정·잔반 대용품)은 **박진용에게 넘기지 않는다** — 오늘 18:30 까지 F3 세 개, 저녁은 F3 실기, 9/22 는 soap·UT-F3·INT-13 으로 여력이 없다. 둘 다 민범진이 계속(책상 작업이 끝났고 로봇 대기 중이라 CELL-03 은 지금 할 수 있다). → SDD §5.4·§6 · 일정표 v10.6 |
| E18 | **세제 수조를 따로 두지 않는다 — 툴 홀더의 비눗물 컵에서 담근다** (9/21 오후 · 황인재) | `soap(count, kind)` 는 툴을 쥔 채 **툴 홀더(비눗물이 담긴 컵) 자리에서 위아래로** 담근다. 그래서 `cell.stations.SOAP.BOWL/CUP` 은 티칭하지 않고 **툴 홀더 좌표 + `f3.soap.depth_mm`(40)** 으로 계산해 넣었다(🟡 BOWL z 105.24 · CUP z 167.27 · F4 브랜치). 🚨 **`depth_mm` 을 바꾸면 SOAP 의 z 도 같이 바꿔야 한다**(박진용 — cell.yaml 주석). 계산의 바탕은 박진용의 "툴·홀더 변경 없음"(E17 ②). 함수 서명·반환은 그대로(IRD §5). 곁가지: **CELL-03 의 수조는 헹굼 수조 1개**만 남는다(세제 수조 불필요 — 민범진) · FR-08 문구 수정. 같은 F4 회신: `WASTE.CUP` = 그릇과 **같은 잔반통** → 같은 posj `[-180, 0, 90, 0, 90, 0]`(이전 값은 615 mm 떨어져 있었고 출처 불명) · `rack.via` = HOME 실측 posx 에서 z 만 338 · 팔레트 그릇 칸 실기 = 칸 바로 위 → z 만 하강 → y −25·z +100 으로 빼기, 오차 0.16~0.24 mm · 오늘 남은 티칭 = **ISOLATE 1곳(그릇·컵 공용)**. → FR-08 · IRD §5 · SDD §5.4 · 일정표 v10.7 |
