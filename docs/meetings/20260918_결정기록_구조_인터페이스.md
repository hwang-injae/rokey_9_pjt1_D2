# 결정 기록 — 실행 구조·인터페이스 (9/18 기준 유효한 결정 전부)

| 항목 | 내용 |
|---|---|
| 일시 | 2026-09-18(금) — 오후 1차 회의(DSN-01, 전원) + 저녁 구조 변경 결정(DSN-02b, PM) |
| 참석 | 한석형 · 민범진 · 박진용 · 황인재 · 기록 황인재(PM) |
| 팀 공유 | ✅ **9/19 아침 브리핑(09:30)에서 전원 확인 — 이의 없음.** 구조 변경(§결정 1~8)과 9/19 부하 재배치(R1~R7) 확정. 브리핑 뒤 PM 점검으로 정한 Q1~Q7·S1~S4는 9/19 저녁 DSN-03에서 확인 |
| 이 문서의 위치 | 9/18에 내린 결정 중 **지금 유효한 것만** 모은 단일 기록이다. 1차 회의에서 정했던 "노드 5개·flow_node만 서비스 호출"은 같은 날 저녁 아래 결정 1~3으로 바뀌었고, 옛 회의록은 삭제했다(git 이력에 있음) |

## 1차 회의(DSN-01)에서 정했고 지금도 유효한 것
| # | 안건 | 결정 |
|---|---|---|
| A | 통합 실행 PC | **2대** (PC-A 로봇 제어 / PC-B HMI). 통신이 안 되면 1대 |
| B | F1↔F3 경계 | **툴 픽업·반납 = F1 `tool()`**, **세제 담금 = F3 `soap()`** |
| C | 닦기 | 그릇·컵은 동작이 달라 분리: `wipe_bowl()`(힘제어 나선) · `wipe_cup()`(삽입·회전·상하) |
| D | 안착·복귀 | 별도 `seat` 없음 — **`place()`가 스펀지 홈이면 안착 놓기**(쥔 채 순응 하강·탐색·`SEAT_FAIL`), 항상 놓기(release)까지. 복귀는 `move_to('HOME', False)` |
| E | 설정 파일 | **2개**: `config/cell.yaml`(공용 좌표·속도·힘 상한·프리셋, 한석형 혼자) + `config/params.yaml`(`f1 f2 f3 flow hmi` 절, 자기 절만) |
| F | `cobot_common` 담당 | ~~박진용 전체~~ → **9/19 네 사람 분담**(아래 "9/19 부하 재배치"). 패키지 정리·리뷰는 박진용, 좌표 값(`cell.yaml`)은 한석형 |
| G | 컵 파지 | **옆면 파지**(V-17 완료). 흔드는 동작은 더 꽉: **파지 힘 2단계 `NORMAL`/`HOLD`**, `shake`·`dip`·`leftover_loop`는 `kind`를 받는다. 전환 방법은 V-23 |
| H | PM | **황인재**: 일정표·문서·`cobot_api`·`cobot_msgs`·런치·노션·제출. 민범진 = F2 + flow + mock + 통합 실행 리더 |

## 아직 정하지 않은 것 → DSN-03 (9/19 저녁)
반납 구역 방식(`RET_B`/`RET_C`·탐색 파지·`EMPTY_ZONE`) · 스펀지 홈 재파지(V-15 결과) · HMI 설계와 `FlowState`/`FlowEvent` 필드·추가 토픽(`/cell/force`·`/cell/grip_width`)·모니터/기록 노드 추가 여부 · 실패 코드·정책 · YAML 키 이름 규칙(제안안 SDD §4.3)

# 구조 변경 결정 (DSN-02b · 9/18 저녁)

## 왜 바꾸는가
두산 파이썬 API(`DSR_ROBOT2`)는 **혼자 위에서 아래로 도는 스크립트**를 전제로 만들어졌다. 로봇 명령마다 자기가 실행기를 돌려 응답을 기다린다. 서비스 콜백 안에서 쓰면 교착하고(TS-01 B), 우회해도 "두 번째 호출부터 멈춤"(TS-01 B′), 프로세스를 나누면 "겹친 명령을 조용히 덮어씀"(TS-01 §7) 같은 함정이 남는다. 코드가 아직 없는 지금 구조를 바꾸는 것이 가장 싸다.

## 비교한 구조 (9/18 저녁, Virtual + 실제 `dsr_controller2`)
| 구조 | 결과 | 판단 |
|---|---|---|
| ① 기능 노드 3개(프로세스 3개) + 각자 DSR 전용 노드 | 번갈아 9회·연속 3회 성공. **다른 프로세스의 명령이 겹치면 드라이버가 덮어쓰고 양쪽 다 성공 응답** | 위험 남음 |
| ② 로봇 서버 노드 1개(기능 노드는 클라이언트) | 9/19 시험: 번갈아 9회 성공, 겹친 요청은 `BUSY`. 단 기능 노드를 기본 `rclpy.spin`으로 만들면 콜백 안 동기 호출에서 교착 | 동작은 하지만 서버 인터페이스(서비스 열몇 개) 신설·힘제어 루프가 서비스를 건넘·중첩 호출 규약 필요 → 이 일정에서는 작업량 큼 |
| ③ 한 프로세스에 기능 노드 3개 + DSR 전용 노드 공유 | 번갈아 9회 성공, 겹친 요청은 `BUSY` 거절 | 절충안 |
| **④ 스크립트형: 기능은 함수, 메인 스레드가 차례로 실행, 통신 노드만 백그라운드** | **연속 6회 성공, 모션 중 상태 2.000 Hz, stop 수락** | **채택** |
| 두산의 클래스 API(`CDsrRobot`) | 같은 대기 방식 + 우리 설치본은 서비스 이름이 달라 스크립트에서도 무한 대기 | 해당 없음 |

④를 고른 이유: 문제가 생길 자리가 없다(두산이 의도한 사용법) · 강사 배포 예제 5개가 모두 이 방식 · 검증된 코드의 비율이 가장 높다 · 우리 시연에 필요한 것(시나리오·HMI·기록·역할 분담)을 잃지 않는다.

## 결정
| # | 항목 | 결정 |
|---|---|---|
| 1 | 노드 | **2개**: `flow_node`(PC-A, 메인 프로그램) · `hmi_bridge`(PC-B). `flow_node` 프로세스 안에 DSR 전용 보조 노드 1개가 자동 생성된다 |
| 2 | 기능 f1·f2·f3 | 노드가 아니라 **파이썬 패키지(함수)**. 패키지·담당·파일 소유는 그대로: `f1_handling.handling` · `f2_sense_flow.sense` · `f3_wipe.wipe` |
| 3 | 기능 사이 약속 | 서비스 12개 → **같은 이름·인자·반환 필드의 함수 12개**. 정본은 새 패키지 `cobot_api`(PM). `cobot_msgs`는 메시지 2개만(v3.0) |
| 4 | flow ↔ HMI | 그대로: `/flow/start|stop|resume`(Trigger) · `/flow/state` · `/flow/event`. 산출물 보강으로 `/cell/force` · `/cell/grip_width` 토픽 제안(DSN-03) |
| 5 | 스레드 규칙 | 두산 함수는 **메인 스레드에서만**. 통신 노드 콜백은 값 저장·깃발만 |
| 6 | 가짜 구현 | `mock_f1_f3` 노드 → **같은 함수 이름의 mock 모듈**(`f2_sense_flow.mock`). `flow.use_mock`으로 전환 |
| 7 | 단독 시험 | 각자 `rig_f*.py` 스크립트에서 `cobot_common.init()` 뒤 자기 함수를 직접 호출 |
| 8 | 검증 항목 | V-20 → "실행 뼈대를 팀 코드로 Virtual 확인"으로 재정의 · V-21 종료(서비스 콜백이 없어져 해당 없음) · **V-24 신설**: 비동기 이동 + 폴링으로 동작 중 소프트 정지·타임아웃 |

## 역할 변화
| 사람 | 바뀌는 것 | 그대로인 것 |
|---|---|---|
| 한석형 | `f1_node` 서비스 5개 → `handling.py` 함수 5개 | 좌표·티칭·`cell.yaml`, 검증 V-01~19, TC-01·02·05·09 |
| 민범진 | `flow_node`가 **메인 프로그램**(통신 노드 + 순서 실행 + 예외 보호 + Ctrl+C 처리) · mock은 모듈로 · `f2_node` 서비스 4개 → `sense.py` 함수 4개 | 상태 머신·실패 정책·기록, 통합 실행 리더 |
| 박진용 | `f3_node` 서비스 3개 → `wipe.py` 함수 3개 · `cobot_common`은 힘 함수(`force.py`) + 패키지 정리·리뷰(9/19 R1로 `init()`·V-24는 황인재에게) | 공용 로봇 함수·힘 함수·안전 파라미터·기구 |
| 황인재 | `cobot_api` 신설·관리 · `cobot_msgs` v3.0 · 런치는 `flow_node` 1개 실행 · 문서·그림·프롬프트·일정표 · (9/19 R1·R4) `cobot_common.init()`(DSR 전용 노드 + 통신 노드, 그리퍼 폭 구독 포함. `spin()`은 필요 없음) · V-24 | HMI 전부(인터페이스가 그대로라 영향 없음) |

## 지금까지 올라온 작업 중 그대로 쓰는 것
| 출처 | 파일 | 판단 |
|---|---|---|
| main | `cobot_msgs`의 `FlowState.msg`·`FlowEvent.msg`, `docs/interfaces/*.msg` | ✅ 그대로 |
| main | `docs/test_logs/20260918_V-02.md`, TS-02, TS-03 (민범진) | ✅ 그대로. TS-03의 "reset은 선택 동작"을 IRD §4·SDD §3.1에 반영 |
| main | `tools/*`, `.github/*`, 시작 가이드·환경설정·기여 규칙 | ✅ 그대로(단어 몇 곳만 수정) |
| `beomjin/…V-02-weigh-precision` | `tools/v02_weigh.py`(서비스 직접 호출 + 타임아웃, 혼자 도는 스크립트) | ✅ 그대로 쓸 수 있다. 구조 ④와 같은 방식 |
| `beomjin/…DSN-03-f2-overview` | F2 개념 정리 문서 | 🟡 "노드·서비스" 표현을 "모듈·함수"로 고치면 그대로 |
| `seokhyung/…INF-02-cobot-common-v0` | `src/cobot_common/robot.py` 함수 뼈대 | 🟡 한석형이 자기 몫(기본 이동·그리퍼)을 `cobot_common/motion.py`로 옮긴다. 힘 함수 뼈대는 박진용의 `force.py`로(두산 API를 아직 import하지 않은 빈 뼈대라 구조와 무관) |
| main | `cobot_msgs`의 srv 12개, `docs/interfaces/*.srv` | ❌ 삭제 → `cobot_api/contracts.py` |

## 후속
| 작업 | 담당 | 언제 |
|---|---|---|
| 9/19 아침 브리핑에서 구조 변경 공유·확인 | 황인재 | ✅ 9/19 오전 완료(이의 없음) |
| DSN-03 2차 회의 — 위 "아직 정하지 않은 것" | 전원 | 9/19 저녁 |
| `git pull` → `cbc` → 에이전트에 AGENTS.md·IRD v3.0·SDD §3.2를 다시 읽힘 | 전원 | 9/19 오전 |
| `cobot_common.init()`(DSR 노드 + 통신 노드) PR — 나머지의 선행 (INF-02a) | 황인재 (9/19 R1로 박진용에게서 넘겨받음) | 9/19 오전 |
| `flow_node` 메인 뼈대(FLOW-01) → mock 모듈(INF-03) | 민범진 | 뼈대 9/19 오전 · mock 9/19 저녁 |
| V-20 팀 코드로 실행 뼈대 확인 | 민범진(+황인재) | 9/19 오후 |

## 9/19 부하 재배치 (PM 결정 · ✅ 아침 브리핑에서 확인, 이의 없음)
구조 변경으로 작업이 늘어 9/19에 네 사람 모두 과부하였다(박진용 오전 9건, 로봇 1대 슬롯 초과). 원칙: **한 사람 한 칸 = 큰 작업(구현) 1개 + 작은 작업(검증·문서) 2개 이내.**

| # | 결정 | 내용 |
|---|---|---|
| R1 | `cobot_common` 분담 | `bootstrap.py`(init·io_node·cfg·shutdown)·`config.py` = **황인재**(Virtual에서 돌려 본 시험 코드를 옮김, 9/19 오전 최우선) · 기본 이동·그리퍼 = **한석형**(V-01·V-22·V-23에서 확인하는 바로 그 동작) · `weigh` = **민범진**(V-02 도구 이식) · 힘 함수 + **패키지 정리·리뷰** = **박진용** |
| R2 | 티칭 2단계 | 1차(9/19 오전): HOME·WEIGH·WASTE·반납 구역·팔레트·수조·격리. 2차(9/20 오전, CELL-04b): 스펀지 홈·툴 홀더 — 기구를 고정한 뒤에야 가능 |
| R3 | 검증을 구현 옆으로 | 구현에 딸린 사전 검증은 그 구현 칸의 첫 단계: V-06↔F1-04, V-08↔F1-03, V-10↔F3-03, V-18↔F3-02, V-07↔F2-01, V-04·V-15↔F1-05. 설계를 정하는 검증(V-01·02·03·23)만 9/19에 남긴다 |
| R4 | 작업 이동 | SAFE-01(안전 자료)·V-24(선택) → 황인재, 기구 제작 CELL-02에 황인재 보조, `fake_state_pub`은 F4-01로, 그리퍼는 강사 배포 Modbus 드라이버 사용(V-05는 드라이버 연결 확인으로 재정의) |
| R5 | 새 작업 | ENV-04 구조 변경 적용(전원) · PKG-01 기능 패키지 골격(S·M·P) · INF-02a bootstrap(H) · INF-02c weigh(M) · CELL-04b 티칭 2차 |
| R6 | 시험 방식 | 함수별 TC는 **구현 직후 바로** 수행(UT 칸은 남은 TC·녹화 마무리). L2는 flow_node + `use_mock`. UT-FLOW에 예외 주입·Ctrl+C |
| R7 | 게이트 | G1 9/19 저녁 → **9/20 오전** · G2 L1 9/20 저녁 → **9/22 오전** · G3 L2 → **9/22 저녁** · G4 L3 → **9/23 오전** · G5 동결 **9/23 저녁 그대로**. 밀리면 SDD §9.9 범위 방어(4개 연속 → 2개, 실패 주입 4종 → 2종) |

## 9/19 일정표 점검 (PM 결정 · DSN-03에서 확인)
일정표 전 시트를 점검해 "같은 일에 주인이 둘", "같은 파일을 세 사람이 수정", "선행 작업보다 앞선 검증", "한 칸 5~7건", "로봇 1대에 3명"을 고쳤다. 일정표 v4.3(`tools/gen/patch_20260919_audit.py`).

| # | 결정 | 내용 |
|---|---|---|
| Q1 | 주인은 한 명 | 담당 표기 `A(B)` = A 주도·B 참여, `A,B` = 같은 이름의 작업을 **각자 자기 몫만**(예 PKG-01). 한 가지 일을 둘이 할 때는 반드시 `주도(참여)`. V-04·V-15 = S(P)(안착은 `place()` = F1, 박진용은 `periodic_search` 제공·참여) · V-16 = M(S) · ENV-03 = H(M) · INT-4b = M(P) · V-20 = M(H) · V-23 = S · V-24 = H · CELL-04 = S(M) |
| Q2 | 팀(구역) = 주도자의 기능 | V-23·V-04 → F1 · V-16 → F2·flow · V-24 → F4 · SAFE-01·V-21 → 전원. INT-13p는 INT-13 P(S)와 같은 일이라 삭제 |
| Q3 | `cobot_common`은 사람별 파일 | `bootstrap.py`·`config.py`·`__init__.py`(재수출) = 황인재 / `motion.py` = 한석형 / `force.py` = 박진용 / `weigh.py` = 민범진. INF-02a PR이 빈 파일 3개를 같이 올리고, 각자 **자기 파일만** 고친다(같은 날 PR 3개가 `robot.py` 하나에서 부딪히는 것을 막음). 부르는 쪽은 그대로 `import cobot_common as cc; cc.move_to()` |
| Q4 | `cell.yaml`은 한석형 혼자 | INF-04(황인재)는 `params.yaml` 절과 `cell.yaml` **키 골격만**. 값은 CELL-04·04b 티칭으로 한석형이 적는다. F1-01은 `cell.yaml`을 읽기만 하는 기능 함수(`f1.move_to` = `cobot_common.move_to`를 감싼 것, `f2.weigh` = `cobot_common.weigh`를 부른 뒤 판정) |
| Q5 | 선후 바로잡기 | V-22(YAML 좌표를 ROS로 재현)는 `move_to`가 있어야 하므로 9/19 오전 → **9/20 오전 티칭 2차 세션 안**. G2(L1, 9/22 오전) 조건에서 UT-F4를 뺀다(UT-F4는 F4-04 뒤 9/22 오후) |
| Q6 | 부하·로봇 슬롯 | INF-04 → 9/19 오후 · INF-03 mock → 9/19 저녁 · V-16 → 9/20 오전 · F4-01 → 9/20 오전 · **F3-03(soap·wipe_cup)을 1칸 → 2칸**(9/20 저녁·9/21 저녁, F3-02는 2칸) · V-10 → 9/20 저녁 · F4-03 → 9/20 저녁·9/21 저녁 · ENV-03 → 9/21 저녁(INT-4·V-13과 한 세션) · UT-F1·UT-FLOW·CR-01 → 9/22 오전. 9/21 저녁 로봇은 1시간씩 교대 F1-04(S) → F3-03(P) → UT-F2(M) — **순서는 DSN-03에서 확정** |
| Q7 | `cobot_common` 작업은 한 묶음 | 일정표에서 INF-02a(1/4 bootstrap, 선행) → INF-02(2/4 motion) → INF-02b(3/4 force) → INF-02c(4/4 weigh)를 **전원 구역 "계약"에 선행 순서로** 모은다. 공용 패키지(cobot_common·config·런치·mock) 작업은 주도자와 상관없이 전원 구역에 둔다(Q2의 예외). INF-02b가 F3 구역에 떨어져 있어 전원 구역에서 a·c만 보인다는 팀원 보고(9/19)로 고침 — 일정표 v4.4 |

## 9/19 F4 점검 보고에 대한 PM 결정 (DSN-03에서 확인)
| # | 결정 | 내용 |
|---|---|---|
| S1 | 🚨 시험은 격리해서 | 모든 PC의 두산 서비스 이름이 `/dsr01/dsr_controller2/*`로 같아, 같은 망·같은 `ROS_DOMAIN_ID`에서는 한 PC의 rig `movej`가 남의 Virtual이나 **실기 컨트롤러**에도 전달된다(겹친 명령은 드라이버가 조용히 덮어쓴다 — TS-01 §7). 로봇에 랜선으로 붙는 PC는 1대뿐이지만, 그 PC도 와이파이로는 다른 PC와 이어져 있어 DDS로는 명령이 들어온다(HMI가 PC-B에서 PC-A의 로봇을 움직이는 바로 그 경로). → **기본은 격리**: 도메인은 전원 60 그대로 두고 `.bashrc`에 `ROS_AUTOMATIC_DISCOVERY_RANGE=LOCALHOST` **한 줄**(개인 도메인 번호는 나누지 않는다 — PM 결정). 실기 컨트롤러·그리퍼 연결은 TCP라 격리 상태에서도 된다. **PC-A↔PC-B 통합 때만** 그 터미널에서 `team60`(SUBNET으로 풀기), 끝나면 `solo`. AGENTS §3 규칙 13, 환경설정 §9 `.bashrc` |
| S2 | Ctrl+C의 주인 | **`cobot_common.init()`이 단독으로** 맡는다(rclpy 기본 SIGINT 처리기를 끔). `flow_node`·`rig_f*.py`는 `try/finally: cc.shutdown()`만 쓰고 `signal.signal`을 걸지 않는다. SDD §3.1·§5.1 |
| S3 | `shutdown()`의 정지 명령 | 설치된 `DSR_ROBOT2.py`에 정지 함수가 없다 → `motion/move_stop` 서비스를 직접 부르는 **최선 시도**. INF-02a의 필수 기준은 "**멈춰 있을 때** Ctrl+C → 깨끗이 종료, 재실행 정상". "움직이는 중 Ctrl+C"는 V-24(선택)에서 확인하고, 안 되면 "브링업 재시작 필요" 로그로 끝낸다. 급한 정지는 Ctrl+C가 아니라 **E-Stop**(SDD §1.5 그대로). INF-02a PR은 이 조사 때문에 늦추지 않는다. → **결과(PR #3, 9/19)**: Virtual에서는 모션 중 Ctrl+C도 정지·재실행이 된다(0.08 s 중단, `move_stop` 0.16 s). 실기 확인과 정지 방식(`DR_QSTOP`) 확인은 V-24·DSN-03 |
| S4 | INF-02a가 올리는 세 파일 | `motion.py`·`force.py`·`weigh.py`는 빈 파일이 아니라 **SDD §3.1 표의 함수 이름·인자만 있고 속은 `NotImplementedError`인 뼈대**. `import cobot_common`은 `init()`보다 먼저 일어나므로 세 파일은 **맨 위에서 `DSR_ROBOT2`를 import하지 않는다** — 두산 함수는 bootstrap이 `init()` 뒤에 내주는 통로로만 쓴다(통로 이름은 INF-02a PR 본문에 적는다). 파일 나누기는 Q3 그대로(그리퍼는 `motion.py` 안, `__init__.py`는 황인재가 만들고 이후 수정은 박진용 리뷰). → **통로 이름(PR #3)**: `cobot_common.bootstrap.dsr()` · 구독 자리는 자기 파일의 `setup_io(node)` · 새 함수는 `__all__`에(SDD §3.1) |
| S5 | PR 검토·승인 | 문서만 바뀐 PR은 Actions 자동 승인·merge. **코드·설정·도구·인터페이스 정본이 포함된 PR은 PM 에이전트가 바뀐 것을 전부 읽고 직접 승인·Squash merge 또는 거절**(황인재 위임, 9/19). 막는 사유 8가지는 CONTRIBUTING §4.1. 위임 범위 밖(저장소 설정·권한·비밀값, 안전에 걸리는데 확신이 없는 것, 새 팀 규칙, PM 에이전트 자신의 변경)은 황인재에게 묻는다. 근거: 기계 검사만으로 19초 만에 merge되던 것을 막는다 |

## 이전 프로젝트(IDC 순찰로봇)와 같은 원칙
로봇에게 명령하는 노드는 하나(`mission_manager` ↔ `flow_node`), 미션의 단계는 그 안의 상태 머신(UNDOCK·NAVIGATE·INSPECT ↔ pick·weigh·wipe·rack), 실제로 움직이는 서버는 제조사 제공(Nav2·Create3 ↔ `dsr_controller2`·그리퍼 드라이버). 이전 프로젝트의 기능별 노드(YOLO·ArUco·Event_engine)는 **감지·판정** 노드였고 로봇에게 명령하지 않았다 — 이번에는 비전이 없어 그 층이 없다. 두산 드라이버는 Nav2와 달리 동기 서비스(취소·피드백 없음, 겹치면 덮어씀)라 명령 경로를 하나로 모아야 안전하다.
