# SAFE-01 안전 파라미터 표 · 위험요소/안전대책 · 예외/오류 목록 — 초안

| 항목 | 내용 |
|---|---|
| 일정표 | **SAFE-01** · 담당 **박진용**(안전 파라미터 담당 · 9/21 재분담) · 황인재 업로드 · 기한 **9/22 오전**(노션 — 강사 산출물 목록) |
| 초안 | PM 에이전트 9/21 — SDD §7·§8 · NFR-01 · AGENTS §3 · `cell.yaml` · `params.yaml` · 트러블슈팅 TS-01~05 · 9/21 아침 실기 결과에서 뽑음 |
| 상태 | 🟢 **박진용 1차 작성 완료(9/21)** — 책상에서 판단할 수 있는 칸은 모두 채웠다. 남은 것은 아래 "실측이 남은 칸" 4줄뿐이고, **컵 세척 검증(9/22 V-10)까지 끝낸 뒤 한 번에 보고**한다 |
| 읽는 법 | "확인" 칸 — **✅ 그대로 써도 된다**(근거 포함) · **⚠️ 고쳐야 한다**(무엇을 어떻게) · **⏳ 다른 담당 값**(안전 담당 의견만 단다) |
| 값 출처 주의 | `cell.limits`·`cell.motion`·`seat` 값은 **F4 브랜치 `injae/20260921-CELL-04b-limits-motion` 에 채워져 있고 아직 main 에 없다**(PR 대기). 그 PR 이 바뀌면 이 표도 맞춘다 |

---

## 0. 실측이 남은 칸 (이것만 남았다 · 9/22 V-10·V-04 에서 채운다)

| # | 칸 | 무엇을 재나 | 재면 무엇이 정해지나 |
|---|---|---|---|
| 1 | `cell.force.force_max_n` **15 → 20** | 컵 바닥 찾기(V-10)의 **실제 접촉 힘** | ✅ **9/21 V-10 실기: 5.1 · 5.7 · 5.6 N**(컵 전용 `f3.wipe_cup.find_limit_n` 5 N). 🔸 컵은 이제 `insert_limit_n`(15)을 **쓰지 않는다** — 15 N 으로 찾았더니 **컵이 스펀지 홈에 쑥 눌렸다**(9/21). → 닦기 쪽 근거로는 20 이 필요 없다. 남은 여유 0 문제는 **F1**(`insert_limit_n` 15 · `seat.contact_limit_n` 15)의 판단이다 |
| 2 | `f3.wipe_*.find_max_mm` **40 → 25** | 바닥까지 **실제로 찾은 거리**(빠른 하강 뒤) | 🔸 9/21 부터 바닥 위치를 미리 정하지 않는다 — HOME 에서 **정한 길이만큼 빠르게**(그릇 135 · 컵 80) 내려간 뒤 힘으로 찾는다. 컵 실측 **찾은 거리 12.8 · 13.0 · 13.1 mm**(4회 모두 13 mm 안팎). 그릇은 9/21 저녁 재검증 뒤 둘 다 25 로 좁힐지 정한다 |
| 3 | `cell.force.compliance_stx` 의 Z | 용기를 쥔 채 순응 ON **전/후 Z 차이**(V-04) | 3 mm 를 넘으면 **안착·삽입만 강성을 따로** 준다(`compliance_on(stx=…)`) |
| 4 | ~~`cell.limits.safe_z_mm` 235~~ | — | ✅ **닦기에는 해당 없음**(9/21). 닦기는 이제 `safe_retreat` 을 쓰지 않고 **툴을 곧게 뽑아 → 온 길을 거꾸로 → HOME** 으로 돌아온다(컵: 뽑은 뒤 z +40 → y −140 → z −40). `safe_z_mm` 은 **soap 에서만** 쓴다 → soap 실기(9/22)에서 확인 |

> 🚨 1·2·4 는 **로봇 시간을 따로 쓰지 않는다** — 어차피 돌리는 닦기 실기와 컵 바닥 찾기 로그에 그대로 찍힌다.
> 절차: [`docs/test_logs/20260921_SAFE-01_실행순서.md`](../test_logs/20260921_SAFE-01_실행순서.md)

---

## 1. 안전 파라미터 표

### 1-1. 속도

| 파라미터 | 값 | 어디서 쓰나 | 근거 | 확인 |
|---|---|---|---|---|
| `run.vel_scale` (환경 변수 `PREWASH_VEL_SCALE`) | **0.3** (첫 실기) | 모든 이동·힘 동작의 속도에 곱한다 | NFR-01 "첫 실기 저속 20~30%" · 실기 rig 는 0.3 을 넘으면 실행 거부 | ✅ 그대로. 9/19~9/21 실기를 전부 0.3 으로 돌렸고 문제 없었다 |
| `cell.limits.vel_free_pct` | 60 % | 빈손 이동 속도 = 100 % 기준 × 이 값 × vel_scale | 가상 시험에서 250 mm/s 넘으면 컨트롤러가 이동을 세움(9/20) → 400 × 60 % = 240 | ✅ 안전 담당 의견 동의(F4 값) |
| `cell.limits.vel_carry_pct` | 30 % | 용기·툴을 든 이동 | 낙하 위험 — 빈손의 절반 | ✅ 동의(F4 값) |
| `cell.motion.vel_tcp_max_mm_s` · `acc_tcp_max_mm_s2` | 400 · 800 | 직선 이동 100 % 기준(셀에서 허용하는 최대) | SDD §4.3 | ✅ `cc.move_arc`·`move_rel` 이 이 값으로 잘라 쓴다 |
| `cell.motion.vel_joint_max_deg_s` · `acc_joint_max_deg_s2` | 100 · 200 | 관절 이동 100 % 기준 | SDD §4.3 | ✅ |
| 🆕 **컵 세척** `f3.wipe_cup.period_s` · `spin_deg` · `stroke_mm` · `cycles` | 3.0 s · 180(±90°) · 15(3 cm) · 5 회 (**주기는 실제 값 — vel_scale 을 곱하지 않는다**) | 컵 안 세척(Move Periodic 한 명령 · 6번 조인트 좌우 + 위아래) | 🔸 **vel_scale 예외 — 결정 E17**(박진용 요청 · 황인재 승인 9/21). 세척은 빠른 속도가 필요하고 실제로 잘 닦이는 것이 목표 | ✅ 9/21 실기. 6번 조인트 최고 188 °/s · 토크 ±0.6 Nm. ⚠️ **0.3 으로 띄워도 세척은 감속되지 않는다** — 값을 바꾸면 `--air` 로 먼저 |
| 🆕 **툴 회전 속도 한계** `f3.wipe_cup.rot_vel_limit_deg_s` | 225 °/s | 로봇 안전 설정의 AngularVelLimit — 세척 회전 최고(2π × 진폭 / 주기)가 넘으면 컨트롤러가 Periodic 을 거절 | 9/21 실기 알람 1212 `Rotation Vel(251.327) exceeds AngularVelLimit(225.000)` | ✅ 코드가 **움직이기 전에** 계산해 넘으면 ROBOT_ERROR |
| 닦기 손목 회전 `f3.wipe_bowl.rot_vel_deg_s` | 400 (× vel_scale) | 벽면 원호의 손목 비틀기 | 더 올리면 경고음(9/20 실기) | ✅ **0.3 에서는 안전**(9/20 14회차 성공 시 경고음 없음). ⚠️ 단 `vel_scale 0.3` 이라 **실제로는 120 °/s 만 나간다** — 정상 운전(1.0)으로 올리는 날 400 이 그대로 안전한지 다시 본다. 그전까지 400 유지 |
| 털기 진폭·주기 `f2.shake` | (V-07 에서 확정) | 잔반 털기·물 털기 관절 왕복 | 충돌 감지 오작동 여부를 V-07 에서 | ⏳ **F2 담당 값**(V-07). 안전 담당 조건 둘만 건다 — ① 진폭·주기를 `params.yaml` 에 두고 코드 상수로 쓰지 말 것 ② 첫 시험은 빈손으로 |

### 1-2. 힘 (NFR-01 · AGENTS §3 규칙 2 — 모든 접촉 동작에 상한·후퇴·타임아웃)

| 파라미터 | 값 | 어디서 쓰나 | 넘으면 | 확인 |
|---|---|---|---|---|
| `cell.force.force_max_n` | **15 N** | 힘 함수 인자의 절대 상한 — 어떤 접촉 동작도 이보다 큰 목표·상한을 줄 수 없다 | 호출 거부(ValueError) | ⚠️ **여유가 0 이다** — `insert_limit_n`(15) · `seat.contact_limit_n`(15) 이 상한과 **같은 값**이라, 15.0 은 정상인데 15.1 이면 호출이 거부된다. 접촉 판정과 절대 상한은 같으면 안 된다 → **20 N 으로 올릴 것을 제안**. 9/22 컵 바닥 찾기(V-10)에서 실제 접촉 힘을 재고 확정한다 |
| 닦기 누르는 힘 상한 `f3.wipe_*.limit_n` | **10 N** | 그릇·컵 닦기 중(공중 기준값 대비) | 즉시 후퇴 → `FORCE_LIMIT` | ✅ 9/20 실기 성공 회차의 실제 누름이 **1.6~2.3 N** 이라 10 N 은 5배 여유. 이상을 잡기에 충분하고 용기를 깨지 않는다 |
| 닦기 옆 힘 상한 `f3.wipe_*.lateral_max_n` | **25 N** | 벽면 돌기·나선·컵 왕복 중 | 즉시 후퇴 → `FORCE_LIMIT` | ✅ 9/19 실측에서 **마찰만으로 옆 힘이 5~14 N 까지 튄다**(V-03 §3-C). 25 는 그 위, 진짜 걸림만 잡는 값이다. 더 낮추면 헛경보가 난다 |
| 닦기 목표 힘 `f3.wipe_bowl.target_force_n` | 1.5 N | 벽면 구간 힘제어(공중 기준값 보정) — 9/20 결정 E13 | — | ✅ 9/20 14회차 확정본 값 그대로. 목표를 3·4·5 N 로 올려도 실제는 1.6·2.0·2.2 N 밖에 안 따라오므로(V-03 2~3회차) 올릴 이유가 없다 |
| 접촉 판정 `cell.limits.contact_limit_n` | **2.0 N** | `contact_down` 이 바닥에 닿았다고 보는 힘(시작 힘 대비) — 수세미가 물러 늦게 올라온다 | 거기서 멈춤 | ✅ **절대값이 아니라 시작 힘 대비 변화량**이어야 한다(PR #24). 툴이 가벼워 공중에서도 Fz 가 실행마다 1.92·2.00·2.34 N 로 달라, 절대값으로 보면 깊이 0.1 mm 에서 오판했다(V-03 6회차) |
| 안착 `cell.beds.*.seat.contact_limit_n` | 15 N | 스펀지 홈 안착 놓기(F1-05) | 들고 후퇴 → `SEAT_FAIL` | ⏳ **F1 담당 값**(V-06). 안전 담당 의견 — 15 는 용기를 깨지 않는 수준이라 괜찮지만, **위 `force_max_n` 과 같은 값이면 안 된다**(여유 0). 상한을 20 으로 올리면 그대로 둬도 된다 |
| 팔레트 삽입 `cell.limits.insert_limit_n` | 15 N | 팔레트 칸 삽입(F1-04) | 후퇴 → `RACK_JAM` | ⏳ **F1 담당 값**(V-09). 🔸 9/21 부터 **컵 바닥 찾기는 이 값을 쓰지 않는다**(아래 줄) — 15 N 이면 컵이 홈에 눌렸다 |
| 🆕 컵 바닥 판정 `f3.wipe_cup.find_limit_n` | **5 N** | 컵 바닥 찾기(`contact_down`, 시작 힘 대비) — **컵 전용** | 거기서 멈춤 | ✅ **9/21 V-10 실기**: 5.1~5.7 N 에서 멈추고 컵이 움직이지 않았다. 15 N 은 컵이 스펀지 홈에 눌렸다 |
| 🆕 컵 세척 중 누르는 힘 `f3.wipe_cup.limit_n` | 10 N | 컵 세척 동작 중 | 즉시 후퇴 → `FORCE_LIMIT` | ✅ 1배 속도 실기 최대 2.1 N. ⚠️ 속도를 1.5·2배로 올렸을 때 **아래로 끌리는 힘**(−8.7 · −11.8 N)으로 걸렸다 — 누름이 아니라 솔이 벽에 끌리는 힘이었고 컵은 안 움직였다. 지금은 방향을 가리지 않고 10 N 으로 본다 |
| 툴 반납 `f1.tool_return_contact_n` | 8 N | 툴 홀더에 내려놓을 때(F1-03) | 못 찾으면 release 하지 않음 → `TOOL_FAIL` | ⏳ **F1 담당 값**(V-25). 안전 담당 의견 — 닫기 전에 바닥을 확인하는 구조라 8 N 이면 충분하고, 낮을수록 안전하다. 동의 |
| 순응 강성 `cell.force.compliance_stx` | [1000, 1000, 200, 100, 100, 100] N/m·Nm/rad | 접촉 구간의 순응 | 수세미 기준값 — 🔔 용기를 쥔 안착·삽입에서는 용기 무게만큼 6~9 mm 처질 수 있다(V-04·V-06 에서 확인) | ⚠️ **닦기에는 이 값이 맞다**(9/20 확정본이 이 값으로 성공). 다만 Z 200 N/m 는 1 N 에 5 mm 처지는 무르기라, **용기를 쥔 안착·삽입에는 그대로 쓰면 안 된다**. 고칠 방법은 이미 있다 — `cc.compliance_on(stx=…)` 이 인자를 받으므로 **동작별로 다른 강성**을 주면 된다. 값은 V-04(9/22)에서 잰다 |
| ~~탐색 하강 10 N~~ | — | NFR-01 에 남아 있으나 **쓰는 곳이 없어졌다**(탐색 파지 → 고정 자리 파지, 9/19·9/20 E9) | — | ✅ **삭제 확인**. 쓰는 곳이 없다(`grep` 으로 확인). NFR-01 문서에서 이 줄을 지워 주십시오 — 남아 있으면 없는 안전장치를 있다고 읽게 된다 |

### 1-3. 시간

| 파라미터 | 값 | 어디서 쓰나 | 넘으면 | 확인 |
|---|---|---|---|---|
| `cell.limits.timeout_s` | 10 s | 접촉 하강 1회(`contact_down`) — **일시 정지한 시간은 세지 않는다**(PR #43) | `TIMEOUT` → 후퇴 | ✅ 실제 하강 1회가 3 mm 걸음으로 40 mm 를 내려가도 몇 초다. 10 s 는 충분하고, **일시 정지 시간을 빼므로** 사람이 멈춰 세워도 헛되이 터지지 않는다 |
| `cell.motion.move_timeout_s` | 30 s | 이동 1번의 상한(일시 정지 시간 제외) | 정지 명령 → `MoveTimeout` → ROBOT_ERROR | ✅ 가장 먼 이동도 0.3 배속에서 10 s 안이다. 30 s 는 3배 여유 |
| 닦기 전체 `f3.wipe_*.duration_s` | 120 s | 그릇·컵 닦기 1회 | `TIMEOUT` → 후퇴 | ✅ **유지**. 9/20 확정본 1회가 바닥 찾기 + 나선 3 s + 벽면 3바퀴로 **25 s 안팎**(9/19 느린 판은 49 s). 120 s 는 넉넉하면서도, 멈춘 채 눌러 붙어 있는 상태를 2 분 안에는 끝낸다 |
| HMI 버튼 응답 `hmi.service_timeout_s` | 1 s | 버튼 → flow 서비스 대답 대기 | "flow 응답 없음" 표시 | ✅ 동의(F4 값) |
| HMI 연결 끊김 `hmi.disconnect_after_s` | 2 s | `/flow/state` 가 안 오면 | 버튼 전부 꺼짐 · 빨간 표시 | ✅ 동의(F4 값) |

### 1-4. 거리·위치

| 파라미터 | 값 | 어디서 쓰나 | 근거 | 확인 |
|---|---|---|---|---|
| `cell.limits.safe_z_mm` | **235 mm** | **접촉 동작 뒤 후퇴 높이**(`safe_retreat`) — 이동 경로에는 쓰지 않는다(9/20 E7) · 🔸 **9/21 부터 닦기는 안 쓴다**(곧게 뽑아 온 길을 거꾸로 HOME) — soap 만 | 컵을 닦던 솔(95 mm)이 컵 입구(용기 안 바닥 33 + 깊이 95 = 128) 밖으로 나오는 높이 223 + 여유. 닦는 자리 접근점과 같다 | ✅ **계산 검산 통과**. 툴 끝점 = 그릇+수세미 68 / 컵+솔 128 → 컵 밖으로 나오는 높이 223, 235 는 12 mm 여유. 실제로 나오는지는 9/22 V-10 에서 눈으로 확인 |
| 이동 도착 확인 | **2 mm / 관절 1°** | `cc.move_to` 가 끝난 뒤 목표에서 이만큼 넘게 떨어져 있으면 `MoveIncomplete`(PR #42) | 9/21 아침 실기에서 WASTE BOWL 의 6 mm 앞 정지를 실제로 잡았다 | ✅ **유지**. 9/21 08시 그릇 한 바퀴(17개 자세) 실측 오차가 **0.11~0.24 mm · 관절 0.01~0.03°** 였다(황인재) → 기준 2 mm 는 **10배 여유**라 헛경보가 날 자리가 없다. ⚠️ 단 `RET_B 슬롯1`(2.78 mm) · `RACK_C2`(2.36 mm·4.72°) 는 **좌표 자체가 어긋나 있는 알려진 문제**다 — 기준을 늘려 덮지 말고 **재티칭**해야 한다 |
| 🆕 빠른 하강 `f3.wipe_bowl.fast_down_mm` · `f3.wipe_cup.fast_down_mm` | 135 · 80 mm | HOME(컵은 컵 위)에서 이만큼 빠르게 내려간 뒤 힘으로 찾는다 | 9/21 실측: HOME 에서 수세미~그릇 바닥 145 · 컵 위에서 솔~컵 바닥 90 → 10 mm 남김 | ✅ 컵 실기에서 남은 10 mm 를 **12.8~13.1 mm** 에 찾았다(측정 오차 3 mm 안) · 🚨 **HOME 을 바꾸면 이 거리가 전부 틀어진다**(팀 공지 9/21) |
| 🆕 컵 위로 가기 `f3.wipe_cup.over_cup_up_mm` · `over_cup_dy_mm` | 40 · 140 mm | HOME → z +40 → y +140 → z −40 (돌아올 때 반대로) | HOME 높이에서 솔 끝이 컵 테두리보다 5 mm 낮아 옆으로 바로 가면 걸린다 | ✅ 9/21 실기 |
| 바닥 탐색 최대 `f3.wipe_*.find_max_mm` | 40 mm | 닦기 바닥 찾기 — 다 내려가도 못 찾으면 중단 | 25 로 줄일지 판단 중 | ✅ **지금은 40 유지**. 컵은 13 mm 안팎에서 찾는다(9/21 실기 4회). 그릇 재검증(9/21 저녁) 뒤 둘 다 25 로 좁힐지 정한다 |
| `f1.place_clear_mm` | 100 mm | 접근점이 없는 자리에 놓은 뒤 위로 빠지는 높이 | 한석형 티칭 스크립트의 "z +100" | ⏳ **F1 담당 값**(V-25·9/21 F1-01 에서 확인 중). 안전 담당 의견 — 되올라오며 용기를 끌고 오는 문제는 원인이 다르니 **이 값을 키워 덮지 말 것** |

### 1-5. 정지

| 파라미터 | 값 | 어디서 쓰나 | 확인 |
|---|---|---|---|
| 정지 종류 `_STOP_MODE` | **1 = DR_QSTOP**(Stop Category 2 — 감속 정지 후 전원 유지) | `bootstrap.py`(Ctrl+C) · `motion.py`(강제정지·이동 시간 초과) | ✅ **DR_QSTOP(1) 이 맞다.** **Stop Category 2 라 감속 정지 뒤에도 서보 전원이 유지**되어, 멈춘 다음에 `force_off`·`safe_retreat` 같은 **복구 명령이 그대로 먹는다**. `DR_QSTOP_STO`(0)는 Category 1+STO 라 전원이 끊겨 복구 명령이 나가지 않고, **접촉 중에 걸리면 힘제어가 켜진 채 툴이 용기 안에 남는다**(TS-05 와 같은 모양). `DR_SSTOP`(2)은 감속 시간이 약 1.5배라 접촉 중에는 그만큼 더 눌린다. `DR_HOLD`(3)는 정지가 아니라 유지다. 근거: 중급교육1 p.105 정지 종류 표 |
| 공식 정지 수단 | ① 웹 HMI **일시 정지**(소프트웨어) ② **티치펜던트 E-Stop**(하드웨어) | 황인재 9/20 결정 · Ctrl+C 는 개발용 | ✅ 동의. 🚨 다만 **일시 정지는 "지금 하던 이동이 끝난 뒤" 멈춘다** — 즉시 멈춰야 하면 **E-Stop** 이다. 이 차이를 시연 대본과 HMI 안내 문구에 넣어 주십시오 |

---

## 2. 위험요소·안전대책

| 위험 | 대책 | 출처 |
|---|---|---|
| 관절 사이 신체 끼임(J3–J4, J1·J2–J5·J6) | 작업 반경 안에 사람 없음 · 첫 실기 속도 30 % · **E-Stop 에 손 닿는 사람 1명 상시** · 실기 rig 는 시작 전에 E-Stop·격리·사람을 묻는다 | SDD §8 · 강의 |
| 툴 날카로운 부분·볼트 풀림 | 손잡이 모서리 처리 · 매 실기 전 그리퍼·툴 볼트 확인 | SDD §8 |
| 잘못된 그립·전원 차단으로 낙하 | 파지 폭 판정 · 들고 있을 때 저속(30 %) · 털기 중 미끄러짐이면 `GRIP_FAIL` → **멈추고 사람이 확인**(9/20 E12) | SDD §7·§8 |
| 🆕 **닦는 중 툴이 그리퍼에서 밀리거나 돌아감**(V-18) | 🟢 **지금까지 실기에서 발생하지 않았다** — 9/19 5·6회차, 9/20 8~11·14회차에서 **수세미·솔을 쥔 채 나선 + 벽면 3바퀴 + 손목 ±18° 비틀기를 반복**했고, 툴이 밀리거나 돌아 동작이 어긋난 회차는 없다. 7회차에서 솔이 벽을 타고 넘어 반지름 82 mm 까지 나간 때조차 **파지는 유지**됐다. → 별도 시험 없이 **닦기 실기 때마다 눈으로 확인**하는 것으로 갈음한다 | V-03 회차 기록 |
| 접촉 동작 중 과도한 힘 | 힘 상한 + 후퇴 + 타임아웃을 **코드 리뷰에서 강제**(막는 사유 ①) · 순응·힘제어는 접촉 구간에서만(규칙 3) | AGENTS §3 |
| 🆕 **안전 높이 경유를 없애 생긴 경로 위험**(9/20 E7) — 로봇이 자세에서 자세로 곧장 간다 | 경로의 안전은 **티칭한 접근점과 이동 순서**가 맡는다 · 실기에서 흐름 순서대로 구간마다 확인(V-22) · 중단(abort) 정리는 **HOME 먼저**(9/20 E11) · 도착 확인(`MoveIncomplete`) | 9/20 결정 · PR #42 |
| 🆕 **손목 특이점**(9/21 실기) — WASTE BOWL(ry 179°)에서 컨트롤러가 이동을 세움 | 특이점(ry 180°)에서 떨어진 자세로 재티칭 · 도착 확인이 멈춘 것을 잡는다 | 9/21 V-22 |
| 🆕 **팔레트에서 빠져나올 때 걸림**(9/21 실기) — 그릇 칸에 접근점이 없어 그리퍼가 팔레트에 걸림 | 칸마다 접근점을 찍고 적재 뒤 **접근점으로 되돌아 나온 다음** HOME(관절 이동) | 9/21 V-22 |
| 털기·물 털기 진폭 | 진폭·속도 YAML 상한 · 충돌 감지 유지 · V-07 에서 확인 | SDD §8 |
| 🆕 `move_spiral` 을 **속도로** 부르면 드라이버(dsr_controller2)가 통째로 멈춤 | **시간(time) + vel·acc 0** 으로만 부르게 공용 함수 `cc.move_spiral` 에 고정(PR #43) | V-03 · TS 후보 |
| 🆕 **이동 실패로 로봇 위치를 모름**(9/21 08:40 — 케이블이 꼬인 채 멈췄는데 시험 도구가 자동으로 HOME 으로 가려 했다) | **이동 실패(위치 불명) 뒤에는 도구·기능 함수·flow 세 곳 모두 자동 이동을 하지 않는다** — 힘·순응만 끄고 사람이 확인한다. 힘 상한은 로봇이 정상이라는 뜻이라 설계대로 후퇴한다(구분한다) | 9/21 사고 · PR #46 · 결정 E11 |
| 🆕 **팔이 쭉 펴지는 자세 · 몸통을 가로지르는 경로**(9/21 — J3 1.7° 자세에서 케이블 꼬임 · 팔레트 충돌) | **팔이 쭉 펴지는 자세(J3 ≈ 0°)를 티칭하지 않는다** · 잔반통(로봇 뒤)과 앞쪽 자리 사이, 팔레트와 다른 구역 사이는 **필요하면 HOME 을 거친다** | 결정 E15 |
| 🆕 **F3 세척 속도가 vel_scale 로 감속되지 않음** | **F3 세척 속도는 vel_scale 예외다(E17) — 0.3 으로 띄워도 닦기(컵 세척 동작)는 감속되지 않는다.** 첫 실기는 `f3.wipe_cup` 속도 값을 직접 낮춰서 시작한다 | 결정 E17 |
| 🆕 **컵 세척 중 4번 조인트가 돌아 손목이 꺾임**(9/21 실기 — Periodic 회전 칸 rx 를 줬다가 부딪힐 뻔) | 회전은 **TOOL rz(그리퍼 축 = 6번 조인트)** 만 쓴다 · **도는 동안 1·4번 조인트를 감시해 1° 넘으면 즉시 정지(QSTOP)** · 6번 조인트 시작 각 ± 90° 가 한계(±350°) 안일 때만 돈다 · 🚨 **Periodic 회전은 Virtual 결과를 믿지 않는다**(Virtual 은 rx ↔ rz 를 뒤바꿔 움직였다) · 새 값은 실기 공중(`--air`)에서 먼저 | 9/21 · `wipe._scrub_cup` |
| 🆕 두산 `DR_Error`(인자 오류) → 그 프로세스의 ROS 가 꺼져 **순응·힘제어가 켜진 채 남음** | 인자는 공용 함수가 먼저 검사 · 남았으면 새 터미널에서 `release_force.py --home` | TS-05 |
| 비상정지 버튼 혼동 | 공식 정지 수단은 둘뿐(웹 일시 정지 · 펜던트 E-Stop) · E-Stop 위치를 브리핑에서 매일 확인 · 웹 버튼 이름은 "일시 정지"(E-STOP 이라 부르지 않는다) | SDD §8 |
| 웹 버튼을 다른 사람이 누름 | 웹 화면은 **이 PC 에서만** 접속(`hmi.host 127.0.0.1`, 9/20 E10) · 버튼 요청에 확인 헤더(F4-02b, 준비됨) | 9/20 결정 |
| 같은 망의 다른 PC 가 우리 로봇에 명령 | 시험은 격리 상태(`solo` = LOCALHOST)에서만(규칙 13) · 실기 rig 가 시작 전에 확인 | AGENTS §3 |
| 안전 매개변수 무단 변경 | 안전 암호는 강사 관리 · 충돌 감도·속도 한계 변경은 **박진용(안전 담당) 승인** | SDD §8 |
| 티치펜던트·ROS 동시 제어 | 티칭 후 제어권 해제 확인 후 브링업 | SDD §8 |
| 액체 | 수조에 물 없음(모션만) · 잔반 대용품은 고형물 | SDD §8 |

---

## 3. 예외·오류 목록

### 3-1. 공정 실패 코드 (기능 함수가 돌려준다 → flow 정책)

| 코드 | 발생 | 처리 | HMI 표시 |
|---|---|---|---|
| `EMPTY_ZONE` | 집는 자리가 비었다 | 구역 종료 → 다음 구역(SKIPPED 기록) | 정보 |
| `LEFTOVER_REMAIN` | 털기 후에도 잔반이 임계 초과 | 격리 → 다음 용기 | 경고 |
| `SEAT_FAIL` | 안착 탐색 한도 초과 | 격리 | 경고 |
| `TOOL_FAIL` | 툴 폭 범위 밖(툴 없음) · 반납 때 바닥을 못 찾음 | 재시도 1회 → 격리 | 경고 |
| `GRIP_FAIL` | 털기·담금 중 미끄러짐 · 무게로 본 빈손 | **PAUSED → 사람이 확인**(9/20 E12) | 오류 |
| `FORCE_LIMIT` · `TIMEOUT` | 닦기·삽입·하강에서 상한·시간 초과 | 즉시 후퇴 → 재시도 1회 → 격리 | 경고 |
| `RACK_JAM` | 팔레트 삽입 걸림 | 후퇴 → 재시도 1회 → 격리 | 경고 |
| `RACK_FULL` | 팔레트 칸 소진 | PAUSED → 팔레트 교체 뒤 재개 | 오류 |
| `ROBOT_ERROR` | 아래 3-2 의 예외 · 두산 오류 · 충돌 정지 | **그 자리 정지 → PAUSED → 사람이 복구**(abort 거부) | 오류 |
| 통신 끊김 | `/flow/state` 2 s 이상 없음 | HMI 버튼 비활성 · 빨간 표시 | 오류 |

### 3-2. 코드 예외 (잡지 않고 위로 → flow 가 처리)

| 예외 | 뜻 | flow 처리 |
|---|---|---|
| `cc.MoveIncomplete` | 이동이 끝났는데 목표에서 2 mm/1° 넘게 떨어져 있다 — **로봇이 어디 있는지 모른다** | ROBOT_ERROR → PAUSED · 재시도·이어 하강 금지 |
| `cc.MoveTimeout` | 이동이 `move_timeout_s` 안에 안 끝남 | ROBOT_ERROR |
| `cc.MotionHalted` | 강제정지(abort)로 이동이 끊김 | **중단 흐름으로**(ROBOT_ERROR 아님) — HOME → 툴 반납 → 격리 → HOME → 다음 용기 |
| `cc.ForceLimitError` · `cc.MotionTimeout` | 힘 함수의 상한·시간 초과 | 기능 함수가 `FORCE_LIMIT` · `TIMEOUT` 으로 바꿈 |
| `KeyError` | `cell.yaml`·`params.yaml` 값이 비어 있다 | **움직이기 전에** 멈춤 → ROBOT_ERROR(문구에 어느 키인지) |
| `ValueError` | 이름·kind·point 를 잘못 줌 · 힘 인자가 `force_max_n` 초과 | 움직이기 전에 멈춤 → ROBOT_ERROR |

### 3-3. 알려진 문제 (트러블슈팅 기록)

| ID | 문제 | 상태 |
|---|---|---|
| TS-01 | 두산 API 초기화 누락 · 서비스 콜백 안 로봇 명령 교착 → **실행 구조를 바꿈**(노드 2개 · 로봇 명령은 메인 스레드 하나) | 종결 |
| TS-02 | 실기 브링업 실패 — 그리퍼 스크립트 실행 권한 누락 | 종결 |
| TS-03 | `reset_workpiece_weight` 호출 후 컨트롤러 서비스 전체 정지 | 회피책 적용 |
| TS-04 | 종료 시 Abort · 종료 로그가 안 남음 | 종결 |
| TS-05 | `DR_Error` → `rclpy.shutdown()` → 순응·힘제어가 켜진 채 남음 | 복구 절차(`release_force.py`) |
