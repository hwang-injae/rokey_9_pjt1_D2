# V-24 실기 확인 — 이동 도중 일시 정지 → 재개 (9/21 저녁)

> 담당 황인재 · 로봇 필요 · 약 30분 · 9/21 저녁 로봇 순서 **②**(① CELL-02b → **② 여기** → ③ 한석형 티칭 → ④ V-22·V-19 → ⑤ V-25 → ⑥ 민범진 → ⑦ 박진용)
> 근거: V-24a 가능성 시험(`20260920_V-24a_정지가능성_황인재.md`) → 구현 PR #34 → E7 PR #42. Virtual 은 10/10 통과했고 **실기는 처음**이다.
> 완료 기준(일정표): **관절 · 직선 · 용기를 든 이동**에서 멈춤 → 재개로 같은 동작 완료 **3회**.

## 0. 시작 전 확인 (하나라도 아니면 시작하지 않는다)

| ☐ | 확인할 것 | 어떻게 |
|---|---|---|
| ☐ | 격리 상태인가 | `rosinfo` → `RANGE=LOCALHOST` (AGENTS 규칙 13) |
| ☐ | `cell.yaml` 의 `limits` · `motion` 값이 채워졌나 | 비어 있으면 이동 함수가 KeyError 로 **아예 안 돈다** — 한석형에게 |
| ☐ | main 최신인가 (E7 · #42 포함) | `cd ~/rokey9_pjt1/rokey_pjt01_ws_f4 && git pull && cbc` |
| ☐ | E-Stop 을 손에 잡았나 | 🚨 **하드웨어 E-Stop 이 공식 비상정지다** |
| ☐ | 로봇 반경 안에 사람·물건이 없나 | HOME 에서 J1 30° · 옆으로 80 mm 움직인다 |
| ☐ | 티치펜던트 제어권을 풀었나 | 안 풀면 브링업이 거부한다 |

```bash
sod && sodreal                                    # 터미널 1 — 실기 브링업 (IP 192.168.1.100)
```

## 1. 절차 — 터미널 2에서 순서대로

스크립트가 **단계마다 멈추고 Enter 를 기다린다.** 로봇을 보고 나서 Enter 를 누른다. 이상하면 `q`.

### 1회차 · 빈손
```bash
cd ~/rokey9_pjt1/rokey_pjt01_ws_f4 && cbc && PREWASH_VEL_SCALE=0.3 python3 src/cobot_common/test/rig_pause.py --real
```

### 2회차 · **용기를 든 채** (그리퍼에 그릇을 쥐여 준 뒤)
```bash
PREWASH_VEL_SCALE=0.3 python3 src/cobot_common/test/rig_pause.py --real --carrying
```

### 3회차 · 2회차를 한 번 더 (같은 함수 연속 3회 — SDD §3.2 ⑧)
```bash
PREWASH_VEL_SCALE=0.3 python3 src/cobot_common/test/rig_pause.py --real --carrying
```

스크립트가 도는 순서(회차마다 4가지):

| 단계 | 무엇을 하나 | 눈으로 볼 것 |
|---|---|---|
| ① 관절 이동 중 일시정지 → 재개 | J1 을 30° 도는 도중 멈춘다 | **그 자리에서 서고**, 재개하면 **이어서** 30° 를 채운다 |
| ② 직선 이동 중 일시정지 → 재개 | 옆으로 80 mm 가는 도중 멈춘다 | 곧게 가다 서고, 이어서 80 mm 를 채운다 |
| ③ 강제정지(halt) | 도는 도중 강제정지 | 그 자리에서 **끝난다**. 풀기 전에는 새 이동이 안 나간다 |
| ④ 멈춰 있을 때 누른 일시정지 | 먼저 눌러 두고 이동을 시킨다 | **출발하지 않다가** 재개 뒤에 출발한다 |

## 2. 기록 (실행하며 채운다)

| 회차 | 상태 | ① 관절 | ② 직선 | ③ 강제정지 | ④ 멈춰 있을 때 | 비고 |
|---|---|---|---|---|---|---|
| 1 | 빈손 | ☐ OK / ☐ FAIL | ☐ / ☐ | ☐ / ☐ | ☐ / ☐ | |
| 2 | 용기 든 채 | ☐ / ☐ | ☐ / ☐ | ☐ / ☐ | ☐ / ☐ | |
| 3 | 용기 든 채 | ☐ / ☐ | ☐ / ☐ | ☐ / ☐ | ☐ / ☐ | |

측정값 (로그의 숫자를 옮겨 적는다)

| 회차 | 누른 뒤 서기까지 움직인 각도 (°) | 멈춘 곳 J1 (°) | 재개 뒤 도달 J1 (°) | 직선 Δy (mm, 목표 80) | 함수가 돌아온 시간 (s) |
|---|---|---|---|---|---|
| 1 | | | | | |
| 2 | | | | | |
| 3 | | | | | |

### 눈으로만 보는 것 (Virtual 에서 확인 못 한 것)

| ☐ | 볼 것 | 왜 |
|---|---|---|
| ☐ | 멈출 때 **덜컥거리지 않는가** | 실기는 관성이 있다. 심하면 `_STOP_MODE`(지금 DR_QSTOP)를 박진용과 다시 본다 |
| ☐ | 용기를 든 채 멈출 때 **용기가 미끄러지지 않는가** | 미끄러지면 파지 힘(프리셋)을 올린다 |
| ☐ | 재개한 경로가 **멈춘 자리에서 이어지는가** (되돌아갔다 오지 않는가) | 되돌아가면 충돌 위험 — 민범진의 flow 재개 정책과 같이 본다 |

## 3. 통과 / 실패

- **통과**: 3회차 모두 ①②가 OK · 용기가 미끄러지지 않음 · 재개 경로가 이어짐 → 일정표 V-24 를 닫는다.
- **실패**: 로그 전체를 저장하고 아래에 적는다. 이 문서 + 로그를 PR 로 올린다.

```
(실패한 경우 여기에 로그·증상·추정 원인)
```

## 4. 막혔을 때

| 증상 | 조치 |
|---|---|
| `KeyError: cell.yaml 의 … 가 비어 있다` | 한석형이 `limits`·`motion` 을 채워야 한다. 그 전에는 **로봇을 안 움직이는 것이 정상** |
| `MoveIncomplete: … 도중에 멈췄다` | 컨트롤러가 이동을 세웠다(속도·관절 한계·특이점). 로봇 위치를 확인하고, `vel_scale` 을 더 낮춰 다시 |
| 일시정지를 눌러도 안 선다 | `check_motion` 폴링이 도는 이동인지 확인. 🚨 힘제어·`move_periodic`(닦는 중)에는 **원래 안 먹는다**(그 동작이 끝난 뒤 다음 이동에서 멈춘다 — IRD §6) |
| 브링업이 조용히 실패 | 포트 12345 잔류 → `killdrcf` 뒤 다시 |
