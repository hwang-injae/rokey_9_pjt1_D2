# INT-4b — 예외 시나리오 실기 대본 (9/29 화 · 오전 리허설 REH-02 뒤 · 14:00 시연 전) · 작성 황인재(F4) 9/23 밤

> 시연 코드 **main 8874dfc**(#105 튜닝 1~3차까지) · 배속 **0.5** · 시나리오마다 **1회** · 통과한 것만 시연에 넣는다(결정 E28).
> 9/23 통합 실기(§21 · 17분 39초)는 **정상 흐름 + 잔반 루프 1회**만 봤다. 여기서는 "잘못됐을 때 로봇·HMI가 약속대로 움직이는가"를 본다.
> 🚨 실기 명령은 한 번에 한 프로세스 · E-Stop 옆에 사람 · 먼저 위험 낮은 것부터(⑤ → ② → ① → ③ → ④).

## 0. 공통 준비 (10분)

| 순서 | 할 일 | 볼 것 |
|---|---|---|
| 1 | `sod && sodreal` (드라이버) | rviz 에 로봇 · 오류 없음. **이게 뜬 뒤에** 다음 |
| 2 | (선택) bag: `mkdir -p ~/rokey9_pjt1/_bags && cd ~/rokey9_pjt1/_bags && soc && ros2 bag record -a -o 0929_INT-4b` | `/flow/state`·`/flow/event` 가 목록에 붙는다(flow_node 뜬 뒤) — 예외 화면 재료(추석 HMI) |
| 3 | `cd ~/rokey9_pjt1/rokey_pjt01_ws_f4 && soc && ros2 launch prewash_bringup prewash.launch.py vel_scale:=0.5 hmi:=true` | 첫 줄 `vel_scale=0.5 · hmi=True` → `문지기 통과` → `케이블 확인 ✅` → `IDLE` → `HMI 서버를 연다`. **가짜 flow(fake_state_pub)는 꺼져 있어야** 한다 |
| 4 | 브라우저 http://localhost:8000 | 연결 점 초록 · 대기 화면 |
| 5 | 배치: 시나리오 카드의 "배치" 대로 | — |

**역할**: 시작·정지·재개·중단 버튼 = 황인재 · ①③ 현장 손 = 한석형(대용품·툴) + 박진용(툴 빼기) · ④ 케이블 = 민범진 · 기록 = PM(로그 저장) · E-Stop = 버튼 안 누르는 사람 1명.

**복구 도구(프로그램이 죽었거나 쥔 채 멈췄을 때 · flow_node 를 Ctrl+C 로 끈 뒤)**
- 보호정지(SAFE_STOP · 로봇 상태 5): 펜던트에서 해제(또는 `set_robot_control 2`). ④ 넛지 경로에서는 코드가 자동 해제 시도(#102).
- `soc && python3 src/cobot_common/test/rig_release.py` — **그리퍼만** 연다(손 넣지 않기 · 새 프로세스는 열고→닫고→다시 연다).
- `soc && python3 src/cobot_common/test/release_force.py --home --up-mm 150` — 힘·순응 끄고 곧게 150 mm 올린 뒤 HOME(0.3배).
- 로봇 위치만 볼 때: `soc && PREWASH_VEL_SCALE=0.3 python3 src/cobot_common/test/rig_coords.py --real --where`(안 움직임).

**HMI 에서 보이는 것(공통)**: 정지되면 단계 표시줄이 멈춘 단계를 가리키고 알람 상자 `일시 정지됨 — X 단계` + flow 메시지 · 로봇 오류면 `🚨 운영자 복구 필요` · 이력 표에 용기마다 DONE/ISOLATED/SKIPPED/ERROR 한 줄 · 넛지 재개 요청이 감지되면 비프 3번(#93 · ④ 경로의 "재개 요청 감지" 메시지에서).

## 1. 순서·예상 시간

| # | 시나리오 | 위험 | 예상 | 담당 |
|---|---|---|---|---|
| ⑤ | 정지·재개·중단 버튼(툴 든 상태 1회) | 낮음 | 8분 | 황인재 |
| ② | 빈 구역 → SKIPPED → HOME(#98 🟡) | 낮음 | 6분 | 황인재 |
| ① | 잔반 과다 → 멈춤(E42) → (a) 덜고 재개 / (b) 중단 → 격리 — 그릇·컵 | 중간 | 20분 | 한석형 |
| ③ | 툴 놓침 → PAUSED → 홀더에 다시 → 넛지/재개 → 재PICK(#99 · flow_node 경로 🟡) — **그릇만** | 중간~높음 | 12분 | 박진용 |
| ④ | 케이블 장력(무게 재는 중 떨림 > 50 g) → PAUSED → 톡톡 → 재검증 → 재개(#93·#102) | 중간 | 10분 | 민범진 |
| ⑥ | (선택) GRIP_FAIL → 멈춤 — 안전하게 유도할 방법 없음 → **제외 권고** | — | — | — |
| ⑦ | (황인재 결정 시) 세제 펌프 `f1.soap_pump.enabled: true` | 🟡 흐름 실기 0회 | 10분 | 한석형 |

합계 약 1시간(복구 포함). 시나리오 사이에 팔레트·용기를 처음 배치로 되돌린다.

## 2. 시나리오 카드

### ⑤ 정지 · 재개 · 중단 버튼 (E11 정리 순서 = 곧게 위로 → HOME → 툴 반납 → 격리 → HOME)
- **배치**: 그릇 1(슬롯 1 · 빈 그릇) · 수세미 홀더 · B1 비움. 시작 → 그릇이 세제(SOAP) 단계에 들어갔을 때(수세미를 든 상태).
- **넣는 법**: ① HMI **일시 정지** → 로봇이 그 자리에서 멈추는지(이동 중이면 바로 · 세제 비틀기(periodic) 중이면 그 구간 끝나고) → 10초 뒤 **재개** → 하던 동작을 이어서. ② 다음 닦기(WIPE) 단계에서 다시 **일시 정지** → **중단**(확인 창 "이 용기를 격리 구역으로 보내고…" → 확인).
- **볼 것**: 정지 로그 `stop 요청 — … 정지` / HMI `일시 정지됨 — 세제 단계` · 재개 로그 `resume — 이어서 진행한다` · 중단 로그 `abort — 이 용기를 접고 다음 용기로 간다` → `중단 정리` 순서: 곧게 위로(safe_retreat) → HOME → **수세미 반납**(툴을 든 채였으므로) → 그릇을 격리 구역(J1 −210 · #90)에 놓기 → HOME → 이벤트 `ISOLATED` → 다음 용기 없음 → `plan 완료 — 그릇 0 · 컵 0 · 격리 1`.
- **통과**: 정지가 5 s 안에 걸림 · 재개 뒤 같은 단계 이어감 · 중단 정리 4단계가 실패 로그 없이 끝나고 수세미가 홀더에, 그릇이 격리 구역에, 로봇 HOME · HMI 이력에 ISOLATED 1줄.
- **실패 시**: 정리 중 한 단계가 실패해도 코드는 계속 치운다(로그 `중단 정리 — … 실패`). 로봇이 이상한 자리에 멈추면 E-Stop → 복구 도구.
- **주의**: 중단은 **PAUSED 에서만** 받는다(먼저 일시 정지). 수조 안에서 중단하면 곧게 올라온 뒤 HOME 으로 간다(9/22 충돌 뒤 규칙).

### ② 빈 구역 → EMPTY_ZONE → SKIPPED → HOME (#98 · 🟡 실기 0회)
- **배치**: 그릇 2(슬롯 1·2) · **컵 구역 두 슬롯 모두 비움** · 팔레트 비움.
- **넣는 법**: 시작. 그릇 2개가 정상 처리된 뒤 컵 구역으로 가서 슬롯 1 빈손(폭 ≈0.4 ✗) → 슬롯 2 빈손 → `EMPTY_ZONE`.
- **볼 것**: 로그 `grip(CUP) … ✗ (빈손·헛잡음)` 2회 → 정책 `next_zone` → 이벤트 **SKIPPED**(kind CUP) → 마지막 구역이 비었으므로 메시지 `반납 구역 확인 완료 — HOME 복귀 중` → HOME → `plan 완료 — 그릇 2 · 컵 0 · 격리 0`. HMI: 컵 슬롯 2개가 빈 표시 · 이력 SKIPPED.
- **통과**: 두 슬롯 확인 뒤 **접근 높이에서 HOME 으로 곧장** 가는 길이 툴 홀더·수조에 안 걸림(🟡 새 경로 · 눈으로) · plan 완료.
- **변형(시간 있으면)**: 그릇 구역만 비우고 컵 2 — 그릇 SKIPPED 뒤 **HOME 을 거치지 않고** 컵 구역으로 가는지(마지막 구역이 아니라 HOME 복귀 없음 · 슬롯 2 접근점 → RET_C 접근점 직선 경로 확인).
- **실패 시**: HOME 복귀 실패면 PAUSED(`EMPTY_ZONE 후 HOME 복귀 실패`) → 사람이 확인 → 재개하면 HOME 재시도.

### ① 잔반 과다 → LEFTOVER_REMAIN → 멈춤(E42 pause) — 그릇 1회 · 컵 1회
- **원리**: 무게 − 기준값 > 50 g 이면 잔반통에서 4회 털고 다시 잰다(최대 2회). 두 번 털어도 50 g 넘게 남으면 `LEFTOVER_REMAIN` → 정책 **pause** → 용기를 **든 채** 무게 자세에서 멈춘다(반납 구역 슬롯 1 위 ≈100 mm).
- **배치**: 대용품 98 g **2개**(≈190 g)를 용기 안에 **테이프로 고정**(털어도 안 나오게) — 또는 고정 없이 넣고, 털기 뒤 재측정 전에 사람이 다시 넣는다(위험 · 고정 권장). **시작 직전 기준값 2종**(`rig_f2 empty --kind BOWL/CUP` · HOME 경유)을 다시 잰다 — 9/23 컵 기준값이 15 → 실측 −49.6 으로 틀어졌었다.
- **넣는 법 (a) 덜고 재개**: 멈추면(로그 `PAUSED — resume 을 기다린다 · 코드 LEFTOVER_REMAIN` · HMI `일시 정지됨 — 무게 단계`) → 한석형이 **테이프를 떼고 대용품을 꺼낸다**(로봇 정지 상태 · 그리퍼는 계속 쥔 채) → 황인재 **재개** → 로그 `재개 — WEIGH 단계부터 다시` → HOME 거쳐 무게 자세 → 다시 재기 → 통과 → 놓기부터 정상 진행 → DONE.
- **넣는 법 (b) 중단**: 컵으로 반복하되 멈춘 뒤 **중단** → 정리: 곧게 위로 → HOME → (툴 없음) → 격리 구역(컵 posj · 9/21 경로) → HOME → 이벤트 ISOLATED → 다음 용기.
- **볼 것**: 첫 무게 ≈ +190 → `털기 1/2` → 재측정 여전히 ≥ 50 → `털기 2/2` → 재측정 ≥ 50 → `LEFTOVER_REMAIN` → PAUSED. 잔반 털기가 **스플라인(한 곡선)** 으로 도는지(3차 튜닝). (a) 재개 뒤 재측정 ≤ 50 통과. (b) 격리 경로가 오른콽 홀더·수조 위를 안 지나는지(J1-only #90 은 그릇 실기 완료 · 컵 posj 경로는 9/21 실기).
- **통과**: (a) 멈춤 → 재개 → DONE 기록 1줄 · (b) 멈춤 → 중단 → ISOLATED 1줄 · 두 경우 모두 용기를 **떨어뜨리지 않음**(E42 전에는 다음 집기에서 떨어뜨렸다).
- **실패 시**: 멈춘 채 아무 버튼도 안 먹으면 Ctrl+C → rig_release(용기가 반납 구역 위라 슬롯으로 떨어진다 — 아래 확인) → release_force --home.
- **주의**: 무게는 흐름 경고(`📉 … 흘렀다`)가 잦다 — 판정이 이상하면 기준값 재측정부터. 대용품 2개가 200 g 근처라 그리퍼 20 N 벽 파지가 견디는지 ①에서 함께 본다(9/23 은 98 g 1개까지).

### ③ 툴 놓침 → TOOL_LOST → PAUSED → 홀더에 다시 → 넛지/재개 → 재PICK → 이어가기 (#99 · flow_node 경로 🟡) — **그릇만**
- **원리**: soap/wipe 가 시작할 때 쥔 폭을 기준으로 삼고, 닦는 동안 폭이 1 mm 넘게 바뀌면 즉시 정지(stop_now) → `TOOL_LOST` → 정책 pause. 재개(HMI **재개** 또는 **넛지 = 로봇을 15 N 이상 0.15 s 살짝 밀기**) → 코드가 `f1.tool PICK` 을 다시 불러 **홀더에서 다시 집고** 닦기 단계부터 이어간다.
- **배치**: 그릇 1(빈 그릇) · 수세미 홀더 · B1 비움.
- **넣는 법 (안전한 방법)**: 닦기(WIPE) 단계 중 **바닥 나선 구간**(로그 `wipe_bowl 나선` 뒤 · 로봇이 순응 상태로 그릇 바닥에서 천천히 14 mm 원을 돈다)에 박진용이 **옆에서** 수세미 손잡이를 잡고 **위로 당겨 뺀다**. 폭이 바뀌는 순간 로봇이 멈춘다(로그 `나선 도는 중 중단: … 놓침 의심` → `TOOL_LOST` → `PAUSED`). 🚨 세제(SOAP) 비틀기 구간·벽면 회전 구간·빠른 하강 중에는 손 대지 않는다. 🚨 **정지 중에 미리 빼 두는 방법은 안 된다** — wipe 가 시작할 때 폭을 새로 재서 놓침을 못 본다.
- **재개**: 박진용이 수세미를 **홀더에 원래 방향으로** 꽂는다 → 황인재 **재개**(1차 · 확실) / 2차로 **넛지**(로봇 손목을 살짝 밀기)로 재개되는지도 한 번.
- **볼 것**: PAUSED 뒤 HMI 알람 `일시 정지됨 — 닦기 단계` · 재개 로그 → `tool` 재PICK: 그리퍼 열기 → **집었던 자리 위로 직선**(그릇 안에서 빠져나올 때 **그릇 테두리에 손가락이 걸리지 않는지** 🟡) → 홀더 자리로 곧게 내려가 집기(폭 25.6) → 로그 `재개 — WIPE 단계부터 다시` → 닦기 처음부터(하강 → 바닥 → 나선 → 벽) → 반납 → 이어서 DONE. 넛지로 재개했으면 로그 `넛지 감지 — 이어서 진행한다` 와 (#102) 로봇 상태 STANDBY 복귀 대기.
- **통과**: 놓침 뒤 0.5 s 안에 정지 · 재PICK 성공(TOOL_FAIL 아님) · 닦기 완주 · DONE 기록. HMI 에 PAUSED → 진행 복귀.
- **실패 시**: 재PICK 이 헛잡으면 `TOOL_FAIL → retry:1 → isolate` 로 정책이 바뀌어 용기가 격리된다(기록 ISOLATED · 시연 규칙상 통과 아님). 로봇이 그릇 안에서 멈춘 채 오류면 Ctrl+C → release_force --home(빈손) 뒤 수세미는 손으로.
- **컵(솔)은 제외**: 컵 닦기는 손목이 180° 를 계속 돌며 위아래로 움직여 손으로 빼기 위험. 시연에는 그릇 TOOL_LOST 만.

### ④ 케이블 장력 이상 → PAUSED → 톡톡 → 재검증 → 재개 (#93 · #102 · 실기 1회)
- **원리**: 무게를 잴 때 30개 표본의 **떨림(최대−최소)이 50 g(`f2.limits.max_weigh_spread_g`)** 을 넘으면 `CableTightError` → 그 자리에서 PAUSED(HMI 메시지 `케이블 상태를 확인해주세요. 확인 후 로봇을 가볍게 톡톡 두드려 주세요.`) → 사람이 **톡톡**(외력 변화 5 N 이상 · `f2.nudge.force_threshold_n`) 또는 HMI 재개 → (#102) 로봇이 SAFE_STOP 이면 자동 해제 → 1.5 s 대기 → 10표본 재측정 → 떨림 ≤ 50 이면 `케이블 상태 정상 확인 — 작업 재개` → 무게 단계부터 다시.
- **배치**: 그릇 1(빈) · 나머지 정상.
- **넣는 법**: 로그 `weigh` 표본을 재는 동안(무게 자세 도착 + 8 s 뒤 약 20 s 창) 민범진이 **그리퍼 케이블을 가볍게 흔든다/당긴다**(로봇 팔은 안 만짐) → 떨림 > 50 g → PAUSED. → 손을 뗀 뒤 3 s → 로봇 손목을 **톡톡**(5 N 넘게 · 세게 치지 않기 — 9/23 실기에서 세게 밀어 SAFE_STOP 이 걸린 적 있음 → #102 가 자동 해제) → 재검증 → 재개.
- **볼 것**: `🔗 weigh(BOWL) 케이블 장력 이상 감지: 떨림 N g > 상한 50 g` → PAUSED · HMI 알람 메시지 · 톡톡 뒤 `👉 톡톡(넛지) 감지` → `재개 요청 감지(유형: nudge)` → **HMI 비프 3번**(#93) → (SAFE_STOP 이면 `recover` 로그) → `케이블 상태 정상 확인(떨림 … ≤ 50)` → 무게 다시 → 정상 진행.
- **통과**: PAUSED 진입 · 톡톡 1~2번으로 재개 · 재개 뒤 무게 정상 · DONE. 톡톡이 안 먹으면 HMI 재개로도 됨(둘 다 확인).
- **실패 시**: 재검증에서 계속 > 50(`케이블 이상 지속`)이면 케이블을 정리하고 다시 톡톡. 로봇이 SAFE_STOP 인데 자동 해제가 안 되면 펜던트.
- **주의**: 9/23 컵 1 무게에서 흔들지 않아도 떨림 45 g 이 나왔다 — 50 은 여유가 적어 **정상 흐름에서 오탐**이 날 수 있다(상한 50 → 80 결정 대기 · PM). 이 시나리오는 "일부러 넘겼을 때 복구되는가" 를 본다.

### ⑥ (선택) GRIP_FAIL → pause — **제외 권고**
- 털기·담금 중 미끄러짐(폭 변화 > 1.0/1.5 mm) 또는 무게 단계 빈손 감지에서 난다. 손으로 안전하게 만들 방법이 없다(용기를 떨어뜨려야 함). 9/23 11:4x 컵 실기에서 오판으로 1회 재현됐고 재개로 이어갔다(§14) — 그 기록으로 갈음.

### ⑦ (황인재 결정 시) 세제 펌프 — `params.yaml f1.soap_pump.enabled: true` (#100 · 기본 false · 흐름 실기 0회)
- 켜면 **모든 툴 PICK 앞**(재PICK 포함)에 HOME → 펌프 위 → 헤드 파지 22 mm → ±90° → 30 mm 힘 누름 10 N → 복귀가 끼어든다. 그릇 1개 흐름으로 1회 · 볼 것: 펌프 헤드 파지 폭 · 누름 힘 로그 · 뒤이은 수세미 집기 정상. 통과 못 하면 false 로 두고 시연 제외.

## 3. 결과 기록표 (실기 뒤 채움 · 시각은 로그 epoch 환산)

| # | 시각 | 넣은 법 | 관찰(로그 핵심 줄 · HMI) | 판정 | 비고·복구 |
|---|---|---|---|---|---|
| ⑤ | | | | | |
| ② | | | | | |
| ①a 그릇 | | | | | |
| ①b 컵 | | | | | |
| ③ 그릇 | | | | | |
| ④ | | | | | |
| ⑦ | | | | | |

## 3-2. 화요일 추가 실기 2개 — 튜닝 값(황인재 9/23 밤 결정 ③④ · 예외 시나리오 뒤 · 로봇 시간 남으면)

둘 다 **시연 코드 값이 아니다** — 화요일 실기에서 통과해야만 넣는다. 실기는 `rig_flow_once --step`(단계마다 Enter · 초 · 관절 각도 로그) 로 용기 1개.

### ③ 홈 B 놓는 방향 0.5° 비켜 두기 — 재파지 회전 223° → 137°
- **뜻**: 그릇을 스펀지 홈 B 에 놓는 자세만 다른 자세들과 손목 방향이 180° 반대라, 닦은 뒤 다시 잡으러 갈 때 손목이 223° 돈다(§19-2 #3). 놓는 자세의 방향(툴 회전 C)을 0.5° 틀어 두면 뒤집는 쪽이 바뀌어 137° 만 돈다(그릇 1개당 3~5 s).
- **바꾸는 값**(cell.yaml · `beds.SPONGE_BED_B.place`): approach_posx 와 posx 의 **C −52.0 → −51.5**(A−C 가 180.2 → 179.7). 두 줄 같이. 다른 값은 그대로.
- **실기**: 그릇 1개 · `PREWASH_VEL_SCALE=0.5 … rig_flow_once.py --kind BOWL --mock "" -n 1 --step`.
  - 3단계(놓기): 그릇을 홈 B 로 옮기며 손목이 **전과 반대 방향**으로 뒤집힌다 — 반납 구역 옆 구조물·슬롯 2 그릇에 안 스치는지 · 홈에 안착(1 mm 어긋남 허용) · 단계 끝 관절 로그 J6 ≈ **−177**(전 +182).
  - 4단계(수세미): 로그 `J6 -40.37° → -220.37°` 로 바뀌어야 정상(가장 가까운 동치가 반대쪽) · 회전 ≈ 43° · 파지 25.6.
  - 8단계(재파지): 홀더에서 홈 B 로 갈 때 손목 회전이 **137° 쯤**(전 223°) · 단계 시간이 전(14.8 s)보다 줄었는지 · 파지 2.1~2.3.
  - 9단계 끝 관절 J6 ≈ **−65**(감김 없음 · +295 면 실패 — RINSE 방향 122.7 과의 궁합).
- **통과**: 3·8단계 충돌 없음 · 12단계 DONE · 9단계 J6 −65 근처. **실패/애매하면 C −52.0 으로 되돌린다**(2줄).

### ④ 컵 옆면 재파지 감시 걸음 3 → 5 mm — 컵 RINSE 84 s 에서 −16 s
- **뜻**: 컵을 옆면으로 다시 잡을 때 마지막 45 mm 를 힘을 보며 내려가는데 한 걸음(3 mm)이 ≈3 s 라 15걸음 ≈ 40 s. 걸음을 5 mm 로 하면 9걸음 ≈ 24 s. 보호(테두리에 얹힘 감지) 범위 45 mm 는 그대로.
- **바꾸는 값**(params.yaml · `f1.regrip_step_mm`): **3 → 5**(코드 자리는 tune2 브랜치 `handling._regrip_step` · main 에 들어와 있어야 함).
- **실기**: 컵 1개 · `--kind CUP --step`. 8단계(재파지)에서: 걸음 수가 로그 간격으로 9번쯤 · 파지 70.3(10 N) · 컵이 밀리거나 기울지 않음 · 단계 시간 전 37.2 s → **≈22 s**. 이어 9·10단계(담금·털기) 폭 유지(미끄러짐 경고 없음).
- **통과**: 8단계 파지 성공 + 시간 단축 + 담금·털기 정상. 실패면 3 으로 되돌린다.

## 4. 시연 반영 규칙 · 열린 결정
- 통과(✅)한 시나리오만 14:00 시연 대본에 넣는다(E28). 🟡·✗ 는 시연에서 **일어나지 않게** 배치한다(빈 구역·대용품 개수·케이블 정리).
- 시연 대본 후보(통과 시): 정상 4개 + 잔반 루프 1회(대용품 1~2개) + 일시 정지·재개 1회. 툴 놓침·케이블·중단은 통과해도 **질문 받으면 보여 주는 예비**로.
- 결정됨(황인재 9/23 밤): **지금 버전(main 8874dfc) 동결 → v1.0-demo 태그(PM)** · 배속 안 적으면 기본 **0.5**(tune2 브랜치 · 동결 뒤 반영 시점 PM) · 홈 B 179.7 과 컵 재파지 5 mm 는 **위 3-2 로 화요일 실기**.
- 열린 결정(황인재): `max_weigh_spread_g` 50 → 80(④ 케이블 오탐 여지) · 세제 펌프 포함(⑦).
