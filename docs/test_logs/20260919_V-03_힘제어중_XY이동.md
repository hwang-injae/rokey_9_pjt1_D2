# V-03 힘제어 중 XY 이동 · 그릇 안쪽 닦기 방식 — 시험 기록 (계속 갱신)

| 항목 | 내용 |
|---|---|
| taskID | **V-03** 힘제어 중 XY 이동·나선 가능 여부 (일정표) |
| 담당 | 박진용 (F3) |
| 기간 | 2026-09-19 저녁 ~ (진행 중, 실기할 때마다 이 문서를 갱신) |
| 로봇 | M0609 실기 · `vel_scale 0.3` · 툴 `Tool Weight` · TCP `GripperDA_v1` |
| 준비물 | 수세미 틀이 아직 없어 **컵용 솔**로 그릇 바닥을 닦았다. 그릇은 HOME 바로 아래에 둔다 |
| 시험 도구 | `src/cobot_common/test/rig_v03.py` (브랜치 `jinyong/20260919-V-03-bowl-wipe`) · 힘 로그 `logs/v03/*.csv` |
| 상태 | 🟢 **동작 확정**(9/20 실기) — 바닥 나선 + 벽면 3바퀴까지 육안 확인. 힘 수치(`cell.force`) PR 과 F3-02 반영이 남음 |

## 0. 무엇을 확인하는 시험인가

1. **힘제어를 켠 채 X·Y 로 움직일 수 있는가** (= 눌러 붙인 채 문지를 수 있는가)
2. 목표 힘을 주면 **실제로 그만큼 누르는가**
3. 그릇 크기를 모르는 채로 **벽을 찾아 벽면까지 닦을 수 있는가** (F3-02 `wipe_bowl` 설계 근거)

## 1. 확정된 닦기 방식 (9/20 실기 확인)

```
HOME → 빠른 하강 135 mm(작업대 상판 1 cm 위)
  → 순응 ON 상태로 3 mm 씩 내려가며 바닥 찾기 (시작 힘 대비 2 N)
  → 찾은 자리 그대로 순응 ON 유지 (더 누르지도, 올리지도 않는다)
  → 바닥: **Move Spiral 한 번** (2.8바퀴 · 반지름 14 mm · 3 s · 좌우 비틀기 없음 · 힘제어 OFF)
  → 힘제어 ON (1.5 N, 공중 기준값 보정)
  → 벽면: 원호(Move C)를 이어 붙여 **반대 방향 3바퀴** + 손목 ±18° 좌우 비틀기
  → 힘제어 OFF → 순응 OFF → 그 높이에서 중심 복귀 → Z 만 올려 HOME
```
- 벽 반지름 = (그릇 안지름 − 툴 지름) / 2 + 벽 누름 = (110 − 90)/2 + 4 = **14 mm**.
  수세미가 물러서 벽에 눌려도 힘이 잘 잡히지 않는다 → 힘으로 찾지 않고 **크기로 계산**하고, 벽 쪽으로 4 mm 밀면서 돈다(9/20 결정).
- 8자(Move Periodic) 방식은 폐기(9/19). 힘으로 벽 찾기도 폐기(9/20).

## 1-1. 강의자료 근거 (추측이 아니라 자료 문장에 맞춘 것)

| 자료 | 반영 |
|---|---|
| 중급1 p.69 "[속도] → **[시간] 지정 옵션**을 이용하여 속도 설정" | `move_spiral` 에 vel 대신 **time**. 속도로 주면 드라이버가 응답하지 않는다 |
| 중급1 p.69 "**최대 반경과 회전 수** 설정에 따라 속도/가속도 오류" | 14 mm 에 7바퀴·1.5 s 는 **시작조차 안 함** → 2.8바퀴·3 s 로 확정 |
| 중급1 p.69 "순응제어 및 비동기 제어를 활용하는 경우가 많다" | 나선은 순응 ON 상태로 돈다 |
| 중급1 p.79 "중첩 가능한 후행 모션: Move L·C·J·JX" (Spiral·Periodic·SX·B 는 불가) | 벽면은 **Move C** 를 radius 로 이어 붙인다 |
| 중급1 p.85 "Move B = 직선·원호를 블렌딩하여 **등속 이동**(구간 최대 100개)" | 검토했으나 이 드라이버에서는 시작되지 않아 쓰지 않는다 |
| 중급2 "Force 전에 **Compliance ON** 필요(없으면 2.1903)" | 순응 → 힘제어 순서 |
| 중급2 "**힘 방향과 같은 방향의 모션 불가**, 다른 방향은 가능(폴리싱)" | 나선 중에는 Z 힘제어 OFF · 벽면(X·Y 이동)에서만 ON |
| 중급2 "비동기·블렌딩 모션 중 순응 시작 시 2.1903" | 순응 켜기 전 `mwait` |
| 중급2 "순응 중 Task 모션만(Move J 계열 금지)" | 세척 구간에는 관절 이동을 쓰지 않는다 |

## 2. 실기 회차별 결과

| 회차 | 일시 | 무엇을 바꿔서 봤나 | 결과 |
|---|---|---|---|
| 1 | 9/19 16:11 | 공중(3 mm 위)에서 상대 모드(REL)로 힘 켜기 → 8자 | ❌ 3 s 안에 바닥까지 못 내려가 **공중에서 8자**. `amove_spiral` 에 `vel` 누락 → `DR_Error` → 순응이 켜진 채 남음([TS-05](../troubleshooting/TS-05_DR_Error_rclpy_shutdown_복구.md)) |
| 2~3 | 9/19 16:30 · 16:38 | **닿은 채** 절대 모드(ABS)로 힘 켜기 · 목표 3·4·5 N | ✅ 접촉 유지. ⚠️ 목표 3·4·5 N → **실제 1.6·2.0·2.2 N** (목표를 올려도 실제가 거의 안 따라온다) |
| 4 | 9/19 17:05 · 17:07 | 힘 켠 채 X·Y 이동(문지르기) | ✅ **V-03 의 핵심 질문에 답: 가능.** 접촉을 잃지 않고 움직였다 |
| 5 | 9/19 17:47~17:54 | 나선 + 손목 비틀기 · 벽을 **반지름 방향 힘**으로 찾기 | ⚠️ 벽을 35 mm 에서 **한쪽에서만** 찾음(계산상 27.5 mm). 2바퀴가 나선 마지막 바퀴와 겹쳐 눈에 안 보임. 닦는 데 49 s |
| — | 9/19 19:00~19:30 | (Virtual, 가짜 벽) 속도·이어 붙이기·반대 방향 2바퀴 확인 | 닦기 49 s → **8.5 s**. 이어 붙이기 4 mm 는 손목 비틀기를 ±2° 로 죽인다(1 mm 면 ±15°) |
| 6 | 9/20 10:18 | 위 수정본으로 실기 | ❌ **바닥 접촉 오판**(깊이 0.1 mm) → 이후 동작이 공중에서 진행. 반지름 4.8 mm 에서 **마찰을 벽으로 오판** |
| 7 | 9/20 10:23 | 벽 판정을 "명령을 못 따라간 거리"로 바꾼 뒤 | ❌ 접촉 오판은 그대로라 솔이 그릇 벽을 **타고 넘어** 반지름 **82 mm** 까지 나감(올라간 높이 3.5 mm) → 사람이 중지. 움직임이 덜컹거리고 소음 |
| 8~11 | 9/20 오후 (수세미로 교체) | 수세미 Ø90 · 걸음·속도·누름을 여러 번 조정 | ⚠️ 작업대가 쿵쿵 울림. 원인 두 가지: ① 걸음마다 힘·위치를 읽어 로봇이 매번 섬(서비스 왕복 30~60 ms) ② 높이를 명령하면서 힘제어와 싸움 |
| 12 | 9/20 15:52 | `move_spiral` 에 시간만 주고 호출 | ❌ 파이썬 API 가 vel 없으면 거부 → `DR_Error` → ROS 가 꺼져 복구 필요. **vel·acc 0 + time** 으로 불러야 한다 |
| 13 | 9/20 16:13 | 나선(반경 14 mm · 7바퀴 · 3 s) | ❌ 반환 0 인데 **시작됨=False**(실제 반지름 0.3 mm). 자료 경고대로 반경·회전 수 대비 속도 문제 |
| — | 9/20 16:18 | 공중(HOME)에서 나선 인자 5조합 시험 | ✅ 5조합 **전부 20 mm 정확히 돌았다**(반경 20 · 3바퀴 · 3 s). 인자 자체는 문제없음 |
| **14** | **9/20 16:43** | **회전 수를 2.8바퀴로(간격 5 mm) · 순응 ON · 힘제어는 벽면에서만** | ✅ **성공.** 나선 실제 반지름 **14.6 mm**(목표 14.0) · 벽면 3바퀴 + 비틀기 · 복귀까지 육안 확인 |
| 15 | 9/20 16:50 | 나선 시간 3 → 1.5 s | ❌ 다시 시작됨=False → **3 s 로 확정** |

## 3. 회차에서 나온 사실 (근거)

### A. 힘제어를 켠 채 X·Y 로 움직일 수 있다 ✅
4회차에서 접촉을 유지한 채 문질렀다. SDD §9.9 의 대안("순응 + 위치 2~3 mm 누르기")은 필요 없다.

### B. 공중에서도 Fz 가 2.0~2.3 N 잡힌다 (툴 무게·센서 치우침)
- 실행마다 1.92 · 2.00 · 2.34 N 로 다르다. 툴이 가벼워 무게 측정이 어려워 **툴 무게 등록으로는 해결할 수 없다.**
- 6회차에서 이 값 때문에 `contact_limit_n = 3` 을 절대값으로 보던 `contact_down` 이 **깊이 0.1 mm 에서 접촉**으로 판정했다.
- → `contact_down` 을 **시작 힘 대비 변화량**으로 고쳤다(PR #24). 목표 힘도 같은 이유로 덜 눌릴 수 있다(§5 남은 것).

### C. 벽은 **힘 크기로 구분할 수 없다**
- 중심에서 바깥으로 갈 때 솔은 거의 **반지름 방향으로 직진**한다 → 마찰이 **안쪽**으로 걸려 벽과 방향이 같다.
- 6회차 실측: 반지름 4.8 mm 에서 안쪽으로 **5.1 N**(마찰) / 5회차 진짜 벽 **5.7 N** — 크기도 겹친다.
- 문지르는 중 옆 힘은 마찰만으로 **5~14 N** 까지 튄다.
- → 벽 판정을 **명령한 반지름을 못 따라간 거리(gap)** 로 바꿨다. 순응(X·Y 3 N/mm)이라 벽에 막히면 실제 위치가 따라오지 못한다.
  막히지 않은 구간의 gap 은 실측 **1.5 mm 이하**(7회차 98 걸음, 평균 0.41 · 최대 1.46) → 기준 **2 mm**.

### D. 솔은 그릇 벽을 **타고 넘을 수 있다**
- 7회차에서 반지름 82 mm(그릇 밖)까지 나가는 동안 gap 은 1.5 mm 이하였다. Z 는 힘제어라 솔이 벽 경사를 타고 올라간다(올라간 높이 3.5 mm).
- → 올라간 높이가 `climb_max_mm`(8 mm)를 넘으면 벽으로 본다(안전 판정). 7회차는 접촉 오판으로 애초에 솔이 바닥에 없었던 것이 근본 원인.

### E. 부드러움 · 속도
- 걸음마다 목표에서 멈추면 뚝뚝 끊긴다 → 목표 앞에서 다음 걸음으로 **이어 붙인다**(blend).
- 이어 붙이기가 크면(4 mm) 손목 비틀기가 상쇄돼 사라진다(Virtual 실측 ±2°) → **손목은 두 걸음에 한 번만** 방향을 바꾸고 이어 붙이기를 키웠다.
- 손목 회전은 M0609 J6 최대(225°/s) 근처라 **회전 속도로는 더 빨라지지 않는다** → 한 걸음·나선 간격을 넓혀 시간을 줄였다.
- 그릇이 스펀지 구멍 안에서 흔들린다(구멍이 그릇보다 크다) → 벽을 세게 밀지 않도록 **벽을 2 mm 만 누른 채** 돈다.

### F. 두산 모션 API 에서 배운 것 (9/20)
- `move_spiral` 은 **속도(vel)로 부르면 드라이버(dsr_controller2)가 통째로 멈춘다** — 다른 서비스도 응답하지 않아 브링업을 다시 켜야 한다. 반드시 `vel=[0,0]·acc=[0,0]` + `time` 으로 부른다.
- 파이썬 API 는 vel 을 생략하면 거부한다(`Invalid value : vel, v` → `DR_Error` → `rclpy.shutdown()`, TS-05).
- **반경 대비 회전 수·시간이 과하면 반환은 0 인데 시작조차 하지 않는다.** 그래서 rig 는 도는 동안 실제 반지름을 보고, 목표의 절반도 못 가면 오류로 멈춘다.
- `Move B`(moveb) 는 이 드라이버에서 시작되지 않았다(반환 0, 움직임 없음) — 쓰지 않는다.
- 걸음마다 힘·위치를 읽으면 **로봇이 매 걸음 선다**(서비스 왕복 30~60 ms). 도는 중에는 위치를 읽지 않고 힘만 가끔 확인한다.

### G. 수세미(스펀지) 때문에 달라지는 것
- 힘이 **늦게** 올라온다 → 접촉 판정 2 N, 걸음 3 mm. 실측 접촉 깊이는 12~17 mm 로 실행마다 다르다.
- 벽에 눌려도 힘이 잘 안 잡힌다 → 벽 찾기를 힘으로 하지 않고 **크기로 계산**한다.
- 힘제어로 목표 힘을 주면 무른 수세미가 계속 파고들어 작업대를 친다 → 목표를 1.5 N 으로 낮추고, 높이는 명령하지 않는다.

## 4. 지금 설정 (실기 확정값, `src/cobot_common/test/rig_v03.yaml` · `rig_v03_config/cell.yaml`)

| 값 | 지금 | 근거 |
|---|---|---|
| 빠른 하강 | 135 mm · 60 mm/s | 작업대 상판 1 cm 위 (상판은 HOME 아래 약 142 mm) |
| 바닥 찾기 | 최대 30 mm · 12 mm/s · **3 mm 걸음** · 접촉 **2 N**(시작 힘 대비) | 수세미가 물러 힘이 늦게 올라온다 |
| 순응 강성 | [1000, 1000, **200**, 100, 100, 100] N/m · Nm/rad | 옆·회전을 물렁하게 해 스틱-슬립 충격 흡수 |
| 바닥 나선 | 2.8바퀴 · 반지름 14 mm · **3 s** · 비틀기 없음 · 힘제어 OFF | 중급1 p.69 |
| 벽면 | 원호 12개(90°) · **반대 방향 3바퀴** · 손목 ±18° · 힘제어 1.5 N | 중급1 p.79 · 중급2 폴리싱 |
| 벽 반지름 | (110 − 90)/2 + **4** = 14 mm | 그릇 크기로 계산, 벽을 4 mm 밀면서 |
| 이동 속도 | 180 mm/s · 회전 120°/s (× vel_scale 0.3) | 회전을 더 올리면 경고음 |
| 힘 상한 | 누르는 힘 10 N · 옆 힘 25 N | NFR-01 · 벽 접촉 여유 |

## 5. 남은 것

| # | 내용 | 언제 |
|---|---|---|
| 1 | `cell.force` 실기 값 PR (그 절만 — 결정 기록 W5) | 9/20~21 |
| 2 | F3-02 `wipe_bowl` 에 이 절차 반영 + 한석형 티칭 좌표(PR #36)로 | 9/21 |
| 3 | V-18 툴 파지 안정성 — 닦는 힘에서 수세미가 그리퍼 안에서 밀리는지 | 9/21 |
| 4 | 닦임 정도 눈으로 확인 후 누르는 힘(1.5 N) 조정 · FR-08 은 3~5 N 이라 근거가 필요하면 PM 과 상의 | 9/21 |
| 5 | 손목 비틀기 각도 — ±18° 에서 ±45~90° 로 키우면 문지르는 거리가 늘어난다(시간도 비례) | 선택 |
| 6 | 🚨 이동 함수가 9/20 비동기로 바뀌었다(PR #34) → 황인재의 이동 함수 실기 확인(9/21 저녁) 뒤 접촉 동작 재확인 | 9/21 저녁 |

## 6. 기록 위치

- 힘 로그 CSV `logs/v03/v03_YYYYMMDD_HHMMSS.csv` — 구간·시각·힘 3축·누르는 힘·명령 위치·실제 위치·손목 각·올라간 높이·못 따라간 거리
  · 경로 그림 `logs/v03/v03_paths_20260919.png`(실기 5회차 vs Virtual) · `v03_bowl200_20260919.png`(안지름 200 mm 가정)
  🔸 `logs/` 는 저장소에 올리지 않는다(.gitignore) — **내 PC 에 있고, 9/23 저녁 동결 때 드라이브에 올린다**(PM 안내)
- 시험 도구와 설정은 브랜치 `jinyong/20260919-V-03-bowl-wipe` (V-03 이 끝나면 정리해 PR)
