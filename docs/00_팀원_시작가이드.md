# 팀원 시작 가이드 — 저장소 받기 → 환경 만들기 → 에이전트 붙이기 → 매일 작업 흐름

> 대상: GitHub·ROS 2가 익숙하지 않은 D-2팀원. **위에서부터 순서대로** 하면 된다. 명령은 터미널에 그대로 복사해 넣는다.
> 예상 시간: GitHub 준비 20분 · 저장소 받기 10분 · PC 환경 1.5~4시간(환경설정 문서) · 에이전트 연결 10분.
> 막히면 30분 넘기지 말고 팀 채널에 "증상 + 명령어 + 오류 전문"을 올린다.

---

## 0. 전체 그림

```
① GitHub 계정·초대 수락·토큰      (한 번)
② 저장소 clone = 내 워크스페이스   (한 번)   ~/rokey9_pjt1/rokey_pjt01_ws
③ PC 환경 (두산 드라이버·별칭)     (한 번)   docs/setup/M0609_환경설정.md
④ 에이전트에 내 프롬프트 붙이기    (한 번)   docs/prompts/F?_이름_프롬프트.md
⑤ 매일: 브랜치 → 작업 → 단위 테스트(녹화) → push → PR → 승인 → merge
```

용어 세 개만 알면 된다.
- **저장소(repo)**: GitHub에 있는 팀 폴더. `https://github.com/hwang-injae/rokey_9_pjt1_D2`
- **clone**: 저장소를 내 PC로 통째로 복사. 복사된 폴더가 곧 우리 ROS 2 워크스페이스 `rokey_pjt01_ws`다.
- **브랜치**: 내 작업용 갈래. `main`은 "언제나 동작하는 정본"이라 직접 고치지 않고, 내 브랜치에서 작업한 뒤 PR(합쳐 달라는 요청)로 넣는다.

---

## ① GitHub 준비 (한 번)

### 1-1. 초대 수락 확인
1. GitHub 로그인 → 오른쪽 위 프로필 → **Your repositories**에 `rokey_9_pjt1_D2`가 보이면 완료.
2. 안 보이면 이메일의 "invited you to collaborate" 메일에서 **Accept invitation**. 메일이 없으면 황인재에게 GitHub ID를 알려 초대받는다.

현재 협업자: 한석형 `Hanseokhyung` · 민범진 `zldzmfoq100-bot` · 박진용 `abyssGo` · 황인재 `hwang-injae`. **공유 계정(`rokey9c2-lgtm`)은 쓰지 않는다.** 커밋은 반드시 자기 계정으로.

### 1-2. 토큰 만들기 (비밀번호 대신 쓴다)
GitHub는 push할 때 계정 비밀번호를 받지 않는다. **개인 토큰(Personal Access Token)** 을 비밀번호 자리에 넣는다.

1. 주소창에 `https://github.com/settings/tokens` 입력 (계정 Settings → Developer settings → Personal access tokens → Tokens (classic))
2. **Generate new token → Generate new token (classic)**
3. Note: `rokey_pjt1` / Expiration: **30 days** 이상(10월 초까지) / 권한: **`repo`** 하나만 체크
4. **Generate token** → `ghp_`로 시작하는 문자열이 한 번만 보인다. **바로 복사**해 안전한 곳(메모장 등, 채팅 금지)에 둔다.

### 1-3. git 설정 (내 PC, 한 번)
```bash
sudo apt install -y git
git config --global user.name "내_GitHub_ID"          # 예: Hanseokhyung
git config --global user.email "내_GitHub_이메일"
git config --global init.defaultBranch main
git config --global pull.rebase false
git config --global credential.helper store            # 토큰을 한 번만 입력하게 저장 (내 노트북에서만)
git config --global --list                              # 확인
```

---

## ② 저장소 받기 = 워크스페이스 만들기 (한 번)

clone 위치는 자유지만, 팀 표준은 `~/rokey9_pjt1/rokey_pjt01_ws`다(문서·별칭이 이 이름을 쓴다).

```bash
mkdir -p ~/rokey9_pjt1 && cd ~/rokey9_pjt1
git clone https://github.com/hwang-injae/rokey_9_pjt1_D2.git rokey_pjt01_ws
cd rokey_pjt01_ws
ls        # AGENTS.md  CLAUDE.md  CONTRIBUTING.md  README.md  docs  src
```

처음 push할 때 아이디(내 GitHub ID)와 비밀번호(= 1-2 토큰)를 물어본다. `credential.helper store`를 했으면 그다음부터는 묻지 않는다.

### 2-1. 워크스페이스 경로를 `.bashrc`에 등록
```bash
echo 'export REWASH_WS=~/rokey9_pjt1/rokey_pjt01_ws' >> ~/.bashrc
source ~/.bashrc && echo $REWASH_WS
```
> 다른 위치에 받았으면 그 경로를 넣는다. 이후 모든 별칭(`cbc` `soc`)이 이 변수를 쓴다.

---

## ③ PC 환경 (한 번) — [docs/setup/M0609_환경설정.md](setup/M0609_환경설정.md)

이 문서를 **1장부터 끝까지** 따라 한다. 핵심만 미리 알면:
- 두산 드라이버는 `~/ws_cobot_pjt/ws_dsr`에 **따로** 받아 빌드한다(강사 배포). 우리 저장소 `src/`에 두산 파일을 넣지 않는다.
- `.bashrc`에 별칭을 넣는다: `sod`(드라이버 source) · `sodvir`(Virtual 브링업) · `sodreal`(실기, PC-A만) · `cbc`(우리 워크스페이스 빌드) · `soc` · `killdrcf` · `port12345`
- 끝나면 문서 맨 아래 **최종 완료 체크리스트**를 전부 체크한다. 이게 일정표의 **ENV-01**이다.

```bash
# 완료 확인 (3줄)
rosinfo            # ROS_DOMAIN_ID=60, dsr PYTHONPATH 보임
sod && sodvir      # RViz에 M0609 + RG2 (Ctrl+C로 종료 후 killdrcf)
cbc                # 우리 워크스페이스 빌드 failed 0
```

---

## ④ 에이전트 연결 (한 번)

### 4-1. 먼저 읽을 것 (30분)
1. [AGENTS.md](../AGENTS.md) — 확정값·시나리오·역할·절대 규칙. 에이전트도 이걸 읽는다.
2. [docs/02_인터페이스_IRD.md](02_인터페이스_IRD.md) — 내 노드가 제공/사용하는 서비스. 혼자 바꾸지 않는다.
3. 내 프롬프트: `docs/prompts/F1_한석형` / `F2_민범진` / `F3_박진용` / `F4_황인재`
4. [CONTRIBUTING.md](../CONTRIBUTING.md) §0~§2 — 브랜치·커밋 규칙

### 4-2. 프롬프트 빈칸 채우기
내 프롬프트 파일을 열어 `______` 네 칸을 채운다: **내 이름 / 수준(예: 파이썬 가능, ROS 2 처음) / GitHub ID / 내 PC(예: GPU 노트북, PC-A 담당)**. 나머지는 손대지 않는다.

### 4-3. 에이전트별 연결 방법

| 에이전트 | 방법 |
|---|---|
| **Claude Code** (추천) | ① Claude Code에서 **폴더 열기 → `~/rokey9_pjt1/rokey_pjt01_ws`** ② `CLAUDE.md`가 `AGENTS.md`를 자동으로 읽는다 ③ 첫 메시지에 **내 프롬프트 파일 전문**을 붙여넣고 보낸다 |
| **Cursor** | ① 같은 폴더를 연다 ② 루트의 `AGENTS.md`를 자동 인식(안 되면 `.cursorrules`로 복사) ③ 채팅 첫 메시지에 프롬프트 전문 |
| **ChatGPT / Gemini (웹)** | ① 새 대화 ② 첨부: `AGENTS.md` + `docs/01~03` ③ 첫 메시지에 프롬프트 전문. 첨부가 안 되면 프롬프트 맨 아래 **부록 A(AGENTS.md 전문)** 까지 그대로 함께 붙여넣는다 |

### 4-4. 첫 응답이 이렇게 오면 정상
```
읽은 파일 목록 → 내 역할·산출물·하지 않는 것 → 제공/사용 인터페이스 표 → STEP 계획표 → "승인할까요?"
```
계획을 읽고 **"STEP 1 진행"** 이라고 답하면 시작한다. 이 순서가 아니면 아래 문구로 되돌린다(프롬프트 맨 아래 "되돌리기 문구" 표).

| 상황 | 이렇게 말한다 |
|---|---|
| 파일 안 읽고 바로 코드 씀 | `규칙 0. 읽은 파일 목록부터.` |
| 여러 STEP을 몰아서 씀 | `규칙 2 위반. STEP 하나만 다시.` |
| 남의 기능(F?) 코드를 만듦 | `규칙 3 위반. 그건 F? 담당이야. mock으로 두고 내 기능만.` |
| 숫자·좌표를 코드에 박음 | `규칙 5. YAML·상대경로로 바꿔서 다시.` |
| 접촉 동작에 힘 상한·후퇴·타임아웃 없음 | `규칙 4. 세 개 넣어서 다시.` |
| 비전·카메라 제안 | `비전 불가. 파지 폭·하중·힘·위치로.` |

### 4-5. 에이전트가 만든 코드는 어디에
`src/<내 패키지>/` 아래 상대경로. 에이전트가 💾 저장 위치와 브랜치·커밋 문구를 같이 준다. 그대로 따라 하되, **실기 로봇을 움직이는 명령은 내가 확인하고 직접 실행**한다(Virtual 먼저, 첫 실기 20~30% 속도).

---

## ⑤ 매일 작업 흐름 (git)

### 5-1. 아침: 최신 main 받기
```bash
cd $REWASH_WS
git fetch origin
git checkout main
git pull origin main
```

### 5-2. 내 브랜치 만들기 — 작업(taskID)마다 하나
taskID는 **구글 드라이브 일정표**의 맨 오른쪽 ID 열(`F1-02`, `INT-13` …). 이름은 전부 영문 소문자·하이픈.
```bash
git checkout -b seokhyung/20260919-F1-02-pick-search      # {이름}/{YYYYMMDD}-{taskID}-{설명}
git branch                                                 # * 표시가 내 브랜치인지 확인
```

### 5-3. 작업 → 단위기능 테스트 → 녹화
- 에이전트와 STEP 진행. 완성되면 **단위기능 테스트**(기준: `docs/03_설계_SDD.md` §9.3의 내 TC).
- 테스트는 **녹화**한다: `YYYYMMDD_TCxx_기능_담당_시도N.mp4` (예 `20260920_TC01_pick_한석형_시도1.mp4`). 영상은 드라이브에 올리고 링크를 `docs/test_logs/YYYYMMDD_TCxx.md`에 적는다.

### 5-4. 저장(commit)하고 올리기(push) — 하루 1회 이상
```bash
git status                                  # 바뀐 파일 확인. build/ install/ log/ 가 보이면 안 됨
git add src/f1_handling/f1_node.py src/f1_handling/config/f1.yaml     # 바꾼 파일만 (git add . 지양)
git commit -m "feat(f1): pick 탐색 파지 — 접촉 하강·폭 판정·재탐색"     # <타입>(<스코프>): <제목>
git push -u origin seokhyung/20260919-F1-02-pick-search              # 처음 한 번 -u, 이후 git push
```
타입은 10개만: `feat fix refactor style docs test chore remove perf ci`. 스코프: `f1 f2 flow f3 f4 common msgs cell bringup docs setup`.

### 5-5. 통합 전: 최신 main을 내 브랜치에 합치기
```bash
git fetch origin
git merge origin/main            # 충돌 나면 5-8
cbc                              # 다시 빌드·통합 테스트
```

### 5-6. PR 올리기 (GitHub 화면)
1. push 직후 터미널에 뜨는 링크를 열거나, 저장소 페이지의 **Compare & pull request** 클릭
2. 제목: 커밋 규칙과 같은 형식 `feat(f1): pick 탐색 파지`
3. 본문 템플릿이 자동으로 뜬다. **"실기 영향" 칸과 단위 테스트 영상 파일명**을 채운다
4. 오른쪽 **Reviewers**에 팀원 아무나 1명 지정 → **Create pull request**
5. 승인이 오면 **Squash and merge** 버튼 → Confirm
6. 브랜치 삭제는 **팀장(한석형) 승인 후에만**. "Delete branch" 버튼을 바로 누르지 않는다.

### 5-7. 리뷰 요청을 받았을 때
PR 페이지 → **Files changed** → 안전(힘 상한·후퇴·타임아웃)·인터페이스 변경·하드코딩만 보고 → **Review changes → Approve** (또는 Request changes + 코멘트). 4시간 안에.

### 5-8. 자주 나는 문제
| 증상 | 원인 | 해결 |
|---|---|---|
| `Password for ...` 에서 실패 | 계정 비밀번호를 넣음 | 1-2 토큰을 넣는다. 저장된 잘못된 값은 `rm ~/.git-credentials` 후 다시 |
| `! [rejected] ... (fetch first)` | 원격이 더 최신 | `git pull origin 내브랜치` 후 다시 push |
| `Need to specify how to reconcile divergent branches` | pull 방식 미설정 | `git config --global pull.rebase false` 후 다시 |
| `CONFLICT (content)` | 같은 줄을 두 명이 고침 | 파일을 열어 `<<<<<<<` `=======` `>>>>>>>` 사이에서 맞는 쪽을 남기고 표시 삭제 → `git add 파일` → `git commit`. 모르겠으면 그 상태로 팀 채널에 |
| 실수로 `main`에서 작업함 | 브랜치 안 만듦 | `git stash` → `git checkout -b 이름/날짜-ID-설명` → `git stash pop` |
| `build/`가 커밋에 들어감 | `git add .` | `git rm -r --cached build install log` → commit. `.gitignore`가 막아주니 원래는 안 들어간다 |
| 다른 팀원 노드가 안 보임 | DOMAIN·네트워크 | 양쪽 `ROS_DOMAIN_ID=60`, 같은 스위치. 환경설정 문서 10-5 |

---

## ✅ 시작 전 체크리스트
- [ ] 초대 수락, 내 계정 토큰 생성, `git config` 3종
- [ ] `~/rokey9_pjt1/rokey_pjt01_ws` clone, `.bashrc`에 `REWASH_WS`
- [ ] 환경설정 문서 최종 체크리스트 통과 (`sodvir` · `cbc` 성공)
- [ ] `AGENTS.md`·IRD·내 프롬프트·CONTRIBUTING §0~2 읽음
- [ ] 프롬프트 `______` 4칸 채움, 에이전트 첫 응답이 "읽은 파일 목록 → … → STEP 계획" 순서로 옴
- [ ] 첫 브랜치 생성 → 첫 push → PR 한 번 연습(문서 한 줄 수정으로 해도 됨)
- [ ] 구글 드라이브 일정표에서 내 taskID·로봇 슬롯 확인, 노션 조별 페이지 접근 확인
